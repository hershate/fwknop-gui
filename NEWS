
*** 最新消息见 ChangeLog 文件或 git 历史 ***
（本文件为 Zurker fork 翻译维护的中文译本；英文原文见 NEWS.en。
  本 fork 各版本的完整改动见 note/release/ 目录。）

===============================================================
本 fork（Zurker 非官方版本）的版本记录
===============================================================

2.9.1 版本（2026-08-25）—— 一键体检成熟化 + 后端解析层修复（42 项提交）
-------------------------------------------------------
 * 一键体检 7 → 14 项：新增 fwknopd 二进制 fork 校验（上游二进制静默
   失效脚枪，概览红色常驻预警 + CLI 一行命令 + 概念条目四表面同判定）、
   磁盘余量、面板监听地址、管理工具、生效授权存在性、TOFU 悬空绑定、
   凭证文件权限（密钥/口令散列文件组或他人可读即预警）。
 * 钻取闭环：未运行/管理工具/预检失败（完整输出 + 错误行锚定）/TOFU
   悬空（预填搜索直达）/审计缺失 全部一键直达处置位。
 * 结论可信度：授权/配置/概览三类拉取失败不再派生误判（统一「无法
   判定」）；并发两轮只认最新轮；15 个变更点自动降级「建议重检」；
   超 24h 旧绿降级琥珀；复制报告补时间戳与版本。
 * 后端修复：明文 HMAC_KEY 写法误判无 HMAC；审计清理与配置备份同秒
   撞名静默覆盖上一备份（防撞序号化）；操作日志导出/列表补全轮转
   上一代（导出拼接、加载更早跨代）。
 * 认证语义：方案导入/导出 401 回登录页附原因、403/429 对齐文案；
   启动日志交代认证形态（未初始化引导/无头令牌）。
 * 回归套件 115 → 124 PASS（近期后端修复全部挂测）。

2.9.0 版本（2026-08-24）—— 排障定位闭环 + 迁移闭环 + 一键体检（80 项提交）
-------------------------------------------------------
 * 排障定位闭环：预检失败按错误文件分流直达出错行（fwknopd.conf 原文
   锚定 / access.conf 查看器跳行）；保存失败原文模式「定位第 N 行」、
   结构化模式按出错行提取指令名 + 编辑器内过滤；access.conf 查看器
   跳行三模式（行号/授权名/#序号），lint 定位自动带入首条 ERROR/WARN；
   授权卡/禁用块行号标注点击直达。
 * 「所搜即所见」高亮收口：事件/用户/TOFU/操作日志/方案五列表 + 事件
   展开详情行 + 配置结构化视图查找，命中全部高亮；已禁用授权区纳入
   搜索；大写前缀搜索高亮修复。
 * 配置备份可见可回滚：自动备份面板内列表 + 一键回滚（联合预检 + 现场
   再备份 + 热加载），回滚前差异预览（bakview 端点双侧掩码）；配置视图
   未收录指令说明回退为原文注释提取（32 个高级指令）；编辑器指令名
   补全扩展至全部指令；「防重放收紧」快速模板。
 * 方案导出/导入迁移闭环：tar.gz 打包下载（含明文密钥，文案明示）↔
   导入（成员白名单 + 大小上限 + 同名拒覆 + 不触碰现役配置）；方案卡
   补快照内容摘要（授权数 + 端口并集，可搜）。
 * 一键体检：预检/进程/审计/防火墙/TOTP 监听一致性/占位密钥/时钟漂移
   7 项聚合，异常前置 + 直达处置 + 原地重跑 + 纯文本报告复制；首次
   运行引导收尾挂体检链接（装完即验）。
 * 进程级日志面板内出口：服务日志查看（journalctl → syslog fallback，
   可刷新/复制）；启动失败后续动作按根因分流；事件页空态区分「尚无
   事件」与「审计未开」。
 * 签发连续作业打磨：成功后服务器地址静默重填/策略选项保留/残留校验
   清理；「仅显示一次」类措辞补找回路径；批量名单分隔符自动拆行 +
   草稿持久化 + 结果行 opt-in 二维码；模板/类似预填覆盖守卫；签发与
   配置保存提示按服务端实报分态（658 审计定稿，杜绝失实「已热加载」）。
 * 撤销/解绑安全预警：单撤销/单台解绑对近期活跃对象琥珀预警，批量
   解绑清单标记在用设备 + 设备别名（15 分钟口径单点化）；授权卡补
   双缺弱姿态徽标（无跳变·无指纹，CSV/搜索同步）。
 * 诊断/操作日志/通知：诊断打包面板别名 + 异常计数构成明细；操作日志
   类别过滤跨刷新持久化 + 钻取统一；通知已读水位入 sessionStorage +
   满 50 条截断口径标注；事件时间窗补「最近 7 天」；事件分页器标注
   500 条截断口径。
 * 回归 115/115 PASS。详见 note/release/2.9.0.md。

2.8.0 版本（2026-08-24）—— 凭证分发闭环 + 审计合规导出 + 并发防护收口（100 项提交）
-------------------------------------------------------
 * 凭证分发闭环：授权 URI 二维码（内嵌 Go 编码器渲染 SVG，签发/重发双入口，
   密钥不离开服务器，失败可重试）；批量签发凭证（名单共享安全选项顺序签发，
   实时校验/逐份复制下载/失败不中断/结果直达操作日志核对）；批量签发/撤销/
   解绑遇会话或权限失败提前终止，剩余按「未尝试」与失败严格区分；签发防呆
   （重名硬拦、& 字符拦截、端口∩TOTP 重叠提示、签发后「盯首次敲门」）。
 * 审计合规导出：access.conf 原文查看（密钥掩码 + 行号/指令名高亮，「第
   X–Y 行」交叉引用闭环）；TOFU 绑定/授权清单/操作日志三路筛选感知 CSV
   导出（访问评审/合规留档）；下载补 Content-Length 真实进度，文件名
   Unicode 感知清洗（中文不再坍缩为下划线）。
 * 操作日志深化为审计问询终端：自由文本搜索 + 行内复制、窗口 100→300→1000
   扩深与到顶终态、失败条目记录并展示原因、类别计数回填、时间制式切换、
   来源 IP 与事件页双向钻取。
 * 登录爆破全链路可见性：概览警示卡 + 非回环监听置顶、关于页导航徽标、
   标签页标题 ‼ 计数、桌面通知（环比上升 + 10 分钟节流 + 点击直达）、
   诊断打包计数、登录回顾灌满告警、非回环警示按地址消除。
 * 并发防护三连收口：修复「轮询外移架空 mtime 乐观锁」（基线锚定编辑器
   打开时刻 + 编辑期外部修改预警条）；stanza 编辑器按字段 diff 提交（并发
   会话他字段不再被盖回，oplog 只记真改动）；方案应用乐观确认（确认前重算
   diff 比对漂移防盲覆盖）；保存被拒/失败现场保留可重试。
 * 事件页正确性：边界秒多重集差去重（同秒同键重放风暴不再被低估）；事件
   筛选方案（类型/搜索/时间窗一键芯片，预制两视角）；五个列表「所见即可搜」
   状态词闭环；防火墙规则结构化视图 + 来源钻取 + 验证放行闭环。
 * a11y 第三批：弹窗 aria-labelledby、SVG 语义两分、侧栏徽标/锚点
   role=button 与计数化 aria-label、值控件指令名标注、会话倒计时摘除
   aria-live 防读屏轮播。
 * 可靠性修复一批：toast 顶出策略/双重转义、偏好与每页条数白名单校验、
   lineDiff 超大文件护栏、断连恢复增量判定、指标速率轴消休眠尖峰、
   窄屏折行、时钟漂移文案厘清等。
 * 回归 115/115 PASS。详见 note/release/2.8.0.md。

2.7.0 版本（2026-08-24）—— 管理面可审计性 + 并发安全 + 编辑器成熟化（39 项提交）
-------------------------------------------------------
 * 管理面可审计性：新增面板操作日志 dashboard_ops.jsonl（0600）——签发/撤销/
   解绑/配置保存/服务控制/方案操作/登录成败/改密等 20 个挂点全动作入档
   （密钥材料永不入档），关于页「面板操作日志」卡（最近 100 条、类别/失败
   过滤、JSONL 导出），超 8MB 单代轮转防爆；登录成功附「上次登录回顾」
   （上次时间/IP + 此后失败尝试数，GitHub 式安全可见性）。
 * 保存并发防护：fwknopd.conf 保存带 mtime 乐观锁——多人同改不再静默互相
   覆盖，冲突拒绝后自动刷新基线；授权编辑带打开时身份核验——并发增删致
   序号移位时编辑不再落到错误的 stanza 上（均向后兼容）。
 * 配置编辑器成熟化：合并式落盘保留原文件注释（自 2.3.0 起首次保存丢注释
   的缺陷修复）+ 保存确认内嵌 diff + 零改动直返；删除改墓碑标记（可撤销、
   仅看修改可见、保存剔除）；脏标记实比对（改回原值自动复位）；重建前收割
   DOM 编辑状态；行级还原。
 * 授权编辑器实时改动清单（modal 内联预览，含死配置剥离口径与改回原值
   复位）；事件排障知识库动作化（6 类拒绝原因的下一步建议内联 + 直达编辑
   器/配置定位/一键 legacy 过渡签发动作）；TOFU 设备别名；撤销授权 TOFU
   悬空预警；签发 VPN 长时放行模板；概览 __CHANGEME__ 前置警示与首次
   运行三步引导。
 * 可靠性修复一批「自始未生效」级缺陷：icon() 双重前缀致全部 JS 图标
   从未显示、旧主题变量残留、初始化密码强度指示被旧函数覆盖、会话临期
   警告 once-flag 不复位；审计尾读改文件偏移增量（轮询 O(文件)→O(增量)，
   半行不消费防漏事件）。回归 115/115 PASS。详见 note/release/2.7.0.md。

2.6.0 版本（2026-08-23）—— 弱网可靠性加固 + 重发 URI 闭环 + a11y 第二批（44 项提交）
-------------------------------------------------------
 * 弱网与宕机可靠性：fetch 统一 12s/20s 超时（AbortController），服务器 hang
   不再挂死轮询；断连指数退避（封顶 60s，倒计时显示真实重试时刻）；浏览器
   离线/上线即刻响应；启动自检失败 5 秒倒数自动重试；写操作网络失败统一
   中文兜底 toast（10 处静默 catch 收口）；unhandledrejection 全局兜底；
   localStorage 写入安全助手收口（隐私模式不再中断交互）。
 * 授权生命周期闭环：新端点 POST /api/admin/qr + 用户卡「重发 URI」两步
   弹窗——cred.json 丢失后为已有授权重建 fwknop:// URI（密钥不离开服务器；
   地址历史点选 + 预填 + 实时校验；URI 参数级掩码显示，复制/下载不受影响）。
 * 可访问性第二批：「跳到主内容」skip link；鉴权错误 role=alert、14 处校验
   提示 aria-live；折叠/切换控件 aria 状态补全；表单标签 for/id 关联；平滑
   滚动尊重系统减弱动效偏好（SB() 收口 13 处）；Esc 链接 入侧栏收栏与通知
   角清空；快捷键标注补齐（分页 ←/→、x、b）；窄栏图标 title 补页面名。
 * 编辑器与表单对称性：授权编辑器输入即校验（端口/重叠/超时/TOTP 范围）+
   死配置联动 + 开放/禁止端口重叠保存拦截 + beforeunload 守卫；签发卡片
   指纹/TOFU 宽限期联动；配置模板新增 NFQ 抓包与 SPA over HTTP（达 5 枚），
   套用后滚动定位闪烁受影响行；授权卡补显禁止端口/超时/强制真实 IP。
 * 时间呈现对时化：事件时间列悬停互换相对/绝对制式；最近敲门、文件更新、
   审计导出等补绝对时刻 title；事件徽章悬停显英文原名；展开行左缘强调条；
   「刚刚刷新」改静态时间戳；「近 15 分钟异常」口径单点化（四处同源）。
 * 杂项：主题三态循环（跟随系统/深色/浅色）；事件搜索 stanza: 前缀；响应
   缓存头显式化（/api/* no-store，HTML no-cache）；非回环监听明文 HTTP 警示；
   修改密码锁定逐秒倒计时；CSV 导出 RFC 4180 引号修补。
   回归 115/115 PASS。详见 note/release/2.6.0.md。

2.5.0 版本（2026-08-23）—— 预警前置 + 键盘可达闭环 + 搜索升级（45 项迭代）
-------------------------------------------------------
 * 预警前置：侧栏用户页琥珀徽标常驻 __CHANGEME__ 占位密钥计数；TOFU 页
   徽标预警悬空绑定（授权已删但绑定残留）；概览状态卡全部可点击直达
   对应明细页。
 * 键盘可达性闭环：KB_CLICK_SEL 统一体系 + 静态元素首扫，复制格/折叠头/
   状态卡/徽标全部纳入 Tab 序列；焦点保护——轮询重渲染不再每 5 秒清空
   Tab 位置（全页面接入）。
 * 搜索与深链：事件深链携带类型筛选与预设时间范围（~f=/~t=），视图链接
   完整还原现场；事件搜索支持前缀词与裸词混用（user:admin port:22）；
   排行芯片新增目标端口维度；方案页搜索过滤（跨刷新记忆）。
 * 配置与方案：编辑器「仅看修改」过滤；原文模式按行过滤+命中高亮；方案
   列表当前生效置顶、支持另存副本（POST /api/profiles/duplicate）；方案名
   前端校验与服务端同源；授权编辑零改动时禁用保存。
 * 操作反馈：批量撤销/解绑/备份清理按钮内实时进度 + 执行中防重；表头
   吸附偏移顶栏高度修复；number 输入滚轮误改修复。
 * 移动端与杂项：技术值输入框禁用自动纠错/大写（23 处），数字框弹数字
   键盘；内联 SVG favicon；theme-color 随主题；主题跨标签页同步。
   回归 112/112 PASS。详见 note/release/2.5.0.md。

2.4.9 版本（2026-08-23）—— 深链现场还原 + 通知降噪 + 宽屏窄栏（50 项迭代）
-------------------------------------------------------
 * 排查现场可分享：事件页深链升级为 #events~bad~q=<搜索词>，「复制视图
   链接」/刷新/前进后退完整还原「仅看异常 + 搜索词」筛选现场；浏览器
   后退在弹窗打开时消费为「关弹窗」（未保存修改守卫可中止导航）。
 * 桌面通知闭环：异常事件桌面通知可点击——聚焦标签页并直达「仅看异常」
   视图；通知中心未读徽标改为只计错误（常规成功回执不再刷屏，标签页
   标题同步为 [N 错误]）。
 * 宽屏与密度：桌面端新增窄侧栏模式（纯图标、状态持久化，宽表格释放
   横向空间）；自动刷新间隔新增 60 秒档；指标卡骨架屏对齐实际 8 张。
 * 校验与防错：配置编辑器重复指令实时琥珀预警（同名后者覆盖前者，静默
   忽略多为误改）；登录/初始化/一致性检查按钮防连点（连按 Enter 不再
   双倍计入登录限流）；快速操作按钮只读切换不再清空悬停说明。
 * 运维可见性：关于页展示面板进程启动时间与已运行时长（/api/overview
   增 panel.started）；新增 -version 标志打印面板版本并加入命令速查；
   服务操作/配置预检结果提示附带耗时；审计备份支持「全部删除」批量
   清理（逐个走白名单端点）。
 * 一致性打磨：确认框按破坏性分级配色（仅删除/撤销/停止等红色，启动/
   保存/热加载恢复主色）；授权编辑弹窗支持 Ctrl+S 保存；已禁用授权区
   折叠状态跨刷新记忆；登录页预告只读模式；事件页 x 键展开/收起本页
   全部详情；搜索框与按钮悬停说明全面补快捷键提示；筛选芯片补
   aria-pressed；方案切换后同步刷新概览。

2.4.8 版本（2026-08-23）—— 自助改密 + 键盘流贯通 + 可见性打磨（50 项迭代）
-------------------------------------------------------
 * 账户与安全：关于页新增自助修改密码（新端点 /api/auth/password：验证
   当前密码 + CSRF + 复用登录限流桶，改密成功即失效其他设备会话，只读
   模式亦可用）；签发结果输出默认掩码密钥行（可一键切换显示）；侧栏
   常驻「只读」模式徽章。
 * 键盘流：用户页 n 直达签发（名称框聚焦）；事件页 b 切换「仅看异常」；
   方案保存表单 Enter 提交；搜索框 Esc 先清空再失焦；选「自定义区间」
   自动聚焦起始时间框；展开事件详情自动滚入视口。
 * 反查与直达：事件详情 stanza 序号旁直接显示授权名；详情用户行反查
   用户页授权；详情「前后 5 分钟」上下文钻取；解绑确认框标注对应授权；
   侧栏异常徽标/未保存圆点点击直达；配置视图行「编辑定位」直达编辑器；
   配置页头部补「存为方案」入口。
 * 数据可见性：事件 CSV 导出补授权名称列并在缓存截断时提示覆盖范围；
   审计备份列表逐文件标注备份时间与大小（/api/overview 增 sizes）；
   已禁用授权列表补禁用时刻（disabled_at）；概览「仅看异常」按钮实时
   标注异常数；通知铃铛悬停显示动态未读数。
 * 校验与提示：配置编辑器开关指令值 Y/N 校验与行内说明即时刷新；签发
   留空服务器地址的占位符后果即时+成功双提示；「允许访问端口」留空
   提示默认 tcp/22；忘记密码重置指引；事件/TOFU 表头列语义悬停；
   服务控制与概览卡片悬停排查指引。
 * 可访问性与响应式：TOFU 排序表头 aria-sort；异常占比条红段/概览汇总
   行键盘可达；窄屏裸表溢出兜底；长值截断防撑破布局。
 * 杂项：命令速查补客户端 setup/knock/profile list/lint 与「面板自身」
   参数组；登录/初始化页版本号动态化（auth/state 同步）。
 * 修复：首访引导条只读模式文案；草稿恢复后校验提示同步；签发卡片
   展开/收起按钮状态同步。
 * 测试：fork 回归 98 → 105 PASS（新增改密 6 用例 + disabled_at 断言）。

2.4.7 版本（2026-08-23）—— 跨配置一致性校验 + 反查闭环 + 可访问性补齐（24 项迭代）
-------------------------------------------------------
 * 跨配置一致性：stanza 编辑器保存前拦截 TOTP_PORT_RANGE 越出
   PCAP_PORT_RANGE（敲门静默丢失）；保存 fwknopd.conf 收窄监听范围时
   确认框琥珀列出受影响授权；签发卡片与事件详情澄清「服务端口 vs
   敲门落点」语义分工。
 * 反查闭环：事件详情反查按钮在目标不存在时禁用并说明（stanza 反查
   先懒加载，修复误报「已不存在」）；TOFU 绑定行内反查对应授权；
   用户页搜索支持 stanza 序号直查；未写入配置时一键复制 stanza 段。
 * 通知与全局感知：标签页标题补未读通知计数；通知历史「新」条目
   标记；toast 正文点击即复制；概览快速操作补「复制诊断信息」；
   时钟偏差警示（TOTP 命门）提到概览顶部。
 * 可访问性：事件时间分布柱键盘可达（Enter/空格钻取）；主题按钮
   aria-pressed；侧栏当前页 aria-current。
 * 事件页：副标题显示筛选匹配数；筛选标签条显示自定义区间具体起止；
   起止倒挂自动交换；计数卡趋势采样不足占位提示。
 * 方案：预览/切换补 access.conf 差异对比（/api/profiles/view 新增
   current_access_conf，密钥掩码）；「完全一致」判定覆盖两个文件；
   卡片补相对时间。
 * 修复：TOFU 批量选择集剔除失效绑定。
 * 测试：fork 回归 98/98 PASS。

2.4.6 版本（2026-08-23）—— 批量操作与一致性打磨（37 项迭代）
-------------------------------------------------------
 * 批量操作：用户页授权批量撤销（勾选 + 批量条，确认框列名并琥珀预警
   近期活跃授权，逐个复用单撤销链路）；TOFU/用户批量条标注「含 N 条
   被筛选隐藏」防误操作。
 * 签发：安全选项分组可折叠（收起后标题行实时显示生效值摘要）；
   签发成功「再签一个」连续签发入口；服务器地址历史支持单条删除；
   「签发类似」按现有授权一键预填；模板芯片高亮与表单实际值保持一致
   （值偏离即撤下高亮）；存为模板同名覆盖即时预警。
 * 事件：排行条带新增用户维度（来源/设备/用户循环）且芯片标注异常数
   （⚠N）；详情行「事件类型」一键同类筛选；复制摘要补齐授权名与
   SPA 端口；自定义时间区间加「最近 24h / 昨日全天」预设；异常占比条
   红色段显示构成明细并可点击直达。
 * 搜索历史通用组件：事件/用户/TOFU/配置（视图+编辑）五个输入框
   datalist 历史建议（Enter/失焦记录，8 条去重置顶）。
 * 全局：浏览器前进/后退翻页（pushState + popstate，与未保存拦截联动）；
   快捷键 t 切换明暗主题；侧栏导航悬停提示快捷键；TOFU 排序表头与
   事件时间列键盘可达；初始化页密码实时校验与强度评估；通知历史导出
   为文本；诊断信息打包补审计备份与近期异常计数。
 * 概览：审计备份堆积超阈值（>20 个或 >100MB）琥珀警示；「导出审计
   日志」实时标注文件大小；指标陈旧警示可点击直达服务控制；服务停止
   横幅内补「立即启动」；方案切换对「与当前完全一致」给出明示。
 * 修复：首访引导条敲门命令改为 fwknop -n 凭证名称（与 import 实际
   行为一致）；批量解绑文案改宽限期条件表述。
 * 测试：fork 回归 98/98 PASS。

2.4.5 版本（2026-08-23）—— 审计备份生命周期 + 排查效率 + 安全硬化（27 项迭代）
-------------------------------------------------------
 * 审计日志管理闭环：面板内一键清理（rename 备份后清空，fwknopd 逐写
   open/close 安全）；备份可下载（/api/audit/download?bak= 白名单防穿越）、
   可见（overview audit_backups + 关于页列表）、可删除（rmbak 端点）。
 * 签发：结果区「下载 cred.json」（URI 前端直转 JSON v1，端到端验证
   fwknop import 逐字节一致）；模板新增「临时排障（5 分钟）」；名称一键
   时间戳命名；按绑定设备数排序；授权名称点击复制；秒值人性化显示。
 * 事件：来源 IP / 设备指纹排行芯片（点击筛选，维度可切换持久化）；
   修复 CSV 导出「事件中文」列错位。
 * TOFU：悬空绑定检测（授权已删的残留绑定琥珀警示）+ 一键全选清理。
 * 配置：编辑器「放弃修改」；新增指令滚动定位聚焦；原文模式行号；
   stanza 编辑器常用值芯片。
 * 安全：toast/通知历史消息改纯文本插入，消除经审计字段注入 HTML 的面。
 * 全局：页面切换滚动位置记忆；自动刷新设置持久化；/ 快捷键覆盖配置页；
   概览计数卡增量徽标（+N）；方案预览配置一键复制。
 * 测试：fork 回归 98/98 PASS（新增审计清理/备份下载/删除等用例）。

2.4.4 版本（2026-08-23）—— 可访问性补齐 + 失败态兜底 + 效率微交互（27 项迭代）
-------------------------------------------------------
 * 无障碍：概览计数卡/摘要卡、cp-cell 复制/筛选单元格全面键盘可达
   （tabindex + Enter/空格 + 聚焦样式）；aria-live 补齐 5 处动态区域；
   全局脚本错误兜底 toast。
 * 事件：详情「复制摘要」一行中文可读文本；异常桌面通知（可选开启，
   后台标签页感知，tag 防刷屏）；表格密度切换（紧凑/舒适，持久化）；
   「筛选中」标签条逐项可见可拆；详情直接解绑已绑定设备。
 * TOFU：表格密度切换（与事件表共用 initDensityToggle，独立持久化）。
 * 配置：视图模式指令查找框（键名/说明过滤，命中分组自动展开）；
   「展开全部/收起全部」；原文编辑行数/字符统计；方案一键时间戳命名。
 * 签发：访问超时/TOFU 宽限/TOTP 端口范围常用值快捷芯片；apply 关闭
   时结果区驻留「未写入配置」提示条。
 * 登录与全局：登录失败提示剩余尝试次数（锁定不再突然）；连接状态区
   点击立即重连；加载失败兜底（骨架屏超时替换为错误占位 + 立即重试，
   9 容器全覆盖）；通知历史「仅看错误」过滤与单条复制；打印/导出 PDF
   样式；品牌区版本号动态化。
 * 测试：fork 回归 90/90 PASS（新增登录剩余次数提示用例）。

2.4.3 版本（2026-08-23）—— 一键签发即生效 + 体验持续打磨（49 项迭代）
-------------------------------------------------------
 * 签发一键生效：新「签发后写入 access.conf 并热加载」选项（默认开启），
   截取 user add 输出的 stanza 段落，经同名查重 → fwknopd 预检 → 备份 →
   原子替换 → SIGHUP 全链路落盘，授权即刻可用；失败不影响签发结果本身。
 * 签发可自定义访问超时：fwknopd-admin user add 新增 --fw-timeout（1-8388608
   秒，与 access.c RCHK_MAX_FW_TIMEOUT 一致），面板表单带换算即时校验。
 * 签发模板支持自定义：当前表单一键「存为模板」，与预制模板同一交互，
   localStorage 持久化，可覆盖/删除。
 * 表单联动：TOTP 关闭时端口范围/端口匹配字段禁用灰化且提交时忽略；
   OPEN_PORTS 常用端口快捷芯片；要求用户名 Unix 惯例校验；名称重名时
   建议可用变体并点击即填。
 * 事件：筛选状态（搜索词/类型/时间窗/自定义区间）持久化；搜索匹配中文
   原因文案与事件标签；说明列点击按原因筛选；详情行 stanza 反查授权、
   指纹反查 TOFU 绑定、端口补齐点击筛选。
 * TOFU：四列点击排序（升降切换+持久化）；搜索支持整串 stanza_key 直贴；
   操作列可复制 stanza_key（CLI unbind 用）。
 * 全局：模态框关闭后焦点返还触发元素；键盘快捷键帮助面板（按 ? 或侧栏
   按钮，与关于页同源渲染）；侧栏事件导航异常徽标、配置未保存圆点、
   版本号动态显示；自动刷新倒计时；标签页标题含当前页与异常计数；
   通知历史会话内持久化；toast 悬停暂停消失；写操作失败自动弹出服务端
   错误文本；注销/离开拦截未保存修改；小屏顶栏精简；登录密码框
   Caps Lock 警示。
 * 测试：fork 回归 89/89 PASS（新增签发自动写入/落盘/同名查重、
   --fw-timeout 自定义与越界拒绝用例）。

2.4.2 版本（2026-08-23）—— WebUI 体验深化（约 60 项 UX 迭代）
-------------------------------------------------------
 * 签发：全字段即时校验（名称格式+唯一性/地址/端口/范围/TOFU 宽限）、
   提交前汇总拦截并聚焦非法字段、模板 hover 预览、草稿含模板状态、
   成功提示带凭证名、结果区滚动定位并聚焦「复制 URI」（键盘流闭环）。
 * 事件：行点击展开全部审计字段（含详情内三维反查与原始 JSON 复制）、
   异常行淡红底色、异常占比条、新事件到达提示条、端口列点击筛选、
   时间列相对/绝对切换、←/→ 翻页、空筛选一键重置、筛选结果 CSV 导出。
 * TOFU：批量解绑（勾选+全选+操作条）、用户名事件反查、列点击复制、
   解绑后续引导、空态检测指纹未知拒绝并给排查入口。
 * 配置：编辑器行级修改高亮（琥珀=已改/绿=新增）、指令值类型校验红框、
   保存前非法值与「有值无键」行拦截、分组可折叠（状态持久化）、
   方案应用确认内嵌 diff、方案名查重防静默覆盖。
 * 概览：计数变动脉冲、放行/拦截率语义着色、指标陈旧警示、
   服务运行时长实时跳动、daemon pill 直达服务控制、首次部署引导条。
 * 全局：通知历史面板（铃铛+未读徽标）、错误 toast 驻留、
   会话剩余时间显示与临期提醒（/api/auth/state 新增 session_exp）、
   断连重试倒计时、模态框焦点捕获、移动端侧栏手势、表格斑马纹。
 * 回归 84/84 PASS（新增 session_exp 用例）。

2.4.1 版本（2026-08-23）—— WebUI 默认管理模式
-------------------------------------------------------
 * 运维面板默认启动即为管理模式（签发/撤销/解绑/配置编辑/服务控制），
   不再需要 `-enable-write`；只读展示改用新增的 `-read-only` 开关。
 * `-enable-write` 保留（默认开）以兼容旧启动脚本。
 * 前端关于页/只读提示/概览卡片措辞同步更新；回归 79/79 PASS。

2.4.0 版本（2026-08-23）—— WebUI 首次启动初始化与强制鉴权
-------------------------------------------------------
 * 运维面板新增首次启动初始化向导：未初始化时所有 API 返回 401
   （needs_setup），须先设置管理员密码（至少 8 位）方可使用。
 * 管理员密码以 PBKDF2-HMAC-SHA256（10 万轮、16 字节盐）存入
   <run-dir>/dashboard_auth.json（0600，原子落盘），面板不输出密码或其散列。
 * 服务端会话登录：32 字节随机令牌，HttpOnly+SameSite=Strict Cookie
   （TLS 下自动 Secure），12 小时过期；新增 /api/auth/state、/api/setup、
   /api/login、/api/logout 端点与前端登录页/注销按钮。
 * 登录限流：每 IP 5 分钟内失败 5 次锁定 60 秒（429）。
 * CSRF 防护：Cookie 会话的写操作必须携带 X-Fwknop-Request 头；
   DASHBOARD_TOKEN Bearer 凭证保留为无头/CI 通道，对全部 /api 有效。
 * 回归测试扩展至 76 项（含初始化/登录/CSRF/限流用例）。

2.3.0 版本（2026-08-23）—— WebUI 全功能化
-------------------------------------------------------
 * 运维面板新增服务控制：启动（强制 `--exit-parse-config` 预检）/停止/
   重启/SIGHUP 热加载/防火墙规则查看（`--fw-list`）。
 * fwknopd.conf 结构化 + 原文可视化编辑；access.conf stanza 在线编辑
   （仅 11 个非密钥指令，密钥行服务端强校验拒绝）。所有落盘遵循
   预检 → 备份 → 原子替换 → 热加载。
 * 配置方案：当前配置快照保存/预览（密钥掩码）/一键应用/删除。
 * 撤销授权一键恢复；审计日志导出；只读模式自动隐藏管理控件。
 * fork 脚本与项目文档全部汉化（含 man 手册页与 GPLv2 非官方译本
   COPYING.zh-CN）。

2.2.0 版本（2026-08-23）—— WebUI 重设计 + 服务端管理闭环
-------------------------------------------------------
 * `fwknopd-admin` 占位命令全部落地：`user list`（密钥掩码）/
   `user rm`（注释禁用+备份+SIGHUP）/`user qr`/`lint`/`tofu unbind`。
 * 运维面板全面重设计并完全汉化：七页侧边栏、明暗主题、自动刷新、
   结构化 TOFU 视图、管理操作包装。

2.1.0 版本（2026-08-13）—— 零信任硬化 + 服务端管理易用性
-------------------------------------------------------
 * 阶段 2：服务端 `PCAP_PORT_RANGE` 端口范围监听（自动生成 BPF）。
 * 阶段 4：access.conf 指纹白名单 + TOFU 绑定 + `REQUIRE_TOTP_PORT_MATCH`；
   结构化 JSON 审计日志 + Prometheus 指标；`fwknopd-admin` CLI 与
   加密凭证/QR 签发；客户端 `fwknop import`。
 * 阶段 6：无 root 回归脚本 `test/run_fork_tests.sh`。

2.0.x（2026-07，fork 内部阶段）—— TOTP 端口跳变 + SPA v4
-------------------------------------------------------
 * 阶段 1：TOTP 引擎（RFC 6238，HMAC-SHA256）+ 客户端端口跳变
   （`--totp-port` 等）。
 * 阶段 3：SPA 协议升级为 4.0.0，可选 device_id 末字段
   （`--device-id`/`DEVICE_ID`），向后兼容 v3。
 * 阶段 5：CLI 易用性——`fwknop setup` 向导/指纹/`knock`/`lint`；
   Windows 便携版托盘客户端。

===============================================================
上游（mrash/fwknop）的历史记录
===============================================================

2.5 版本（2013-06-16）的重要变更

 * HMAC 认证加密支持（对 Rijndael 和 GPG 均有效）

 * 更新了 fwknop 使用 Rijndael 的方式，包括使用 PBKDF1 算法进行密钥
   派生、修复截断缺陷、更谨慎地处理密码学操作中使用的缓冲区等等。

 * fwknop 代码库现已通过优秀的 Coverity 静态分析产品审计，并因此修复了
   大量缺陷。完整细节见 git 日志。

 * 测试套件现在可以把 fwknop 对 SPA 数据的加密与认证同 OpenSSL 交叉比对。
   这是一个强有力的验证手段，因为它实际上让 fwknop 得以利用 OpenSSL 库
   及其背后的全部研究成果，而无需直接链接 OpenSSL。（OpenSSL 的功能远
   超 fwknop 所需。）

 * 通过把加密密钥、HMAC 密钥和命令行参数保存到 ~/.fwknoprc 文件，实现
   了新的工作流。只需使用 stanza 名称即可访问远程服务
   （'$ fwknop -n <stanza 名称>'），大大简化了 fwknop 的命令行用法。

fwknop-2.5 之前的重要变更见 ChangeLog。
-------------------------------------------------------

0.62 版本（2009-07-26）的重要变更
-------------------------------------------------------

 * 首次公开发布

 * 支持 GPG 加密（经由 gpgme）。

 * 除 UDP 外，SPA 数据包还可经 TCP、TCP（raw socket）和 ICMP 发送。

 * 新增 FKO Perl 模块（包装 libfko）。

 * 新增对 Win32 平台的支持。

 * 较为完整的 libfko Texinfo 文档。


0.0.0-alpha 版本（2008-12-21）的重要变更
-------------------------------------------------------

 * 开始从 Perl 移植到 C

 * 创建了最初的 libfko.a 静态库

 * fwknop 客户端程序已能生成可被 Perl 版 fwknopd 服务端解析的简单访问
   请求。


 fwknop 按 GNU 通用公共许可证（GPL v2+）的条款分发。

 版权所有 2009-2013，Damien Stuart

 分发本文件是希望它能有用，但在法律允许的范围内不附带任何担保；
 甚至没有对适销性或特定用途适用性的默示担保。
