
（本文件为 Zurker fork 翻译维护的中文译本；英文原文见 README.en。）

本目录包含 fwknop 测试套件。在 test/ 的上级目录中编译好 fwknop 源代码
之后即可执行测试套件。test/configure_build.sh 脚本为 'configure' 脚本
提供了推荐的默认参数，应在编译前使用。此后，执行测试套件最全面的方式
之一是：

# ./run-test-suite.sh --enable-all

该模式会启用 IP 解析测试，因此需要互联网接入。如果不方便或不需要联网，
可以使用另一种同样全面的测试模式：

# ./run-test-suite.sh --enable-recompile --enable-perl-module-checks --enable-distcheck

关于测试套件的更多细节（包括 --diff 和 --enable-valgrind 模式）见
fwknop 教程：

http://www.cipherdyne.org/fwknop/docs/fwknop-tutorial.html#3.5

如果想获得比 --enable-all 更全面的测试，可以使用 --enable-complete。
不过在此之前，你需要安装 'libfiu' 故障注入库（见
http://blitiri.com.ar/p/libfiu/），然后用
'test/configure_max_coverage.sh' 脚本编译 fwknop。该脚本为 'configure'
提供额外参数，以最大化测试与代码覆盖率的方式构建 fwknop。编译完成后
运行：

# ./run-test-suite.sh --enable-complete

注意：此模式下测试套件将在 test/output/ 目录中占用接近 500MB 的磁盘
空间。占用磁盘的主要来源是 python SPA 报文模糊器
'test/spa_fuzzing.py' 的使用。

---

补充（Zurker fork）：本 fork 另有一套**无需 root** 的回归脚本
`test/run_fork_tests.sh`，覆盖构建、单元测试、REF C 测试、阶段 2/4
端到端以及 WebUI 全部 API（当前 61/61 PASS）：

# ./test/run_fork_tests.sh
