
***
请注意：本 Android 客户端已被弃用，请改用 Jonathan Bennett 的新 Android
客户端，见：

https://github.com/jp-bennett/Fwknop2/releases/
****

（本文件为 Zurker fork 翻译维护的中文译本；英文原文见 README.en。）

===============================================================================
FWKNOP for Android（Android 版 FWKNOP）

Max Kastanas <max2idea@users.sourceforge.net>

基于 Damien Stuart 的 fwknop C 实现
===============================================================================

- 对 fwknop 项目的 Android 开发感兴趣的读者：./project/sdk.paths 文件用于
为本地 JVM 以及 Android SDK 和 NDK 的安装提供 JAVA_HOME 和 PATH 定义。
一般工作流程是：首先把 sdk.paths 文件中的路径更新为指向你系统上的正确
目录；你还需要同样更新 ./android/project/ant.properties 文件中的
sdk.dir 和 ndk.dir 路径。然后：

$ source ./project/sdk.paths

- 然后，启动 Android SDK：

$ android &

- 现在，如果要实现新功能或修复缺陷，就对 fwknop/android/project/ 下的
文件进行修改。然后，在 Android 模拟器运行时：

$ cd ./project
$ ant clean
$ ant debug
$ adb install -r bin/fwknop-android-debug.apk

 - 或者 -

$ cd ./project
$ ant clean
$ ant release
$ adb install bin/fwknop-android-release-unsigned.apk

- 新的 Android 应用装进模拟器后，你就可以启动它并用它发送 SPA 数据包。


### 以下是旧版说明： ###

1. 需求：

    a. Apache Ant (1.7.1)
        http://ant.apache.org/bindownload.cgi

    b. Android NDK（Android r5-linux）
        http://developer.android.com/sdk/ndk/index.html
        把内容解压到某个目录，例如：
        /home/yourname/tools/android-sdk-linux_86

    c. Android SDK（Android r08-linux）
        http://developer.android.com/sdk/index.html
        把内容解压到某个目录，例如：
        /home/yourname/tools/android-ndk-r5

    d. 把 Android SDK 的 tools 目录加入你的用户 PATH 变量：
        export PATH=$PATH:/home/dev/tools/android-sdk-linux_86/tools:/home/yourname/tools/android-ndk-r5

    e. 下载一个 SDK Android 平台：在命令行中键入 "android &" 启动
       "Android SDK and AVD Manager"，在 "Available Packages" 一节下载
       一个 SDK 平台（推荐 Android 2.1 及以上）。

    f. Java SDK（1.6 及以上）
        http://www.oracle.com/technetwork/java/javase/downloads/index.html

    g. Netbeans（6.9 及以上）——如果你想用 Netbeans 作为 IDE 则为可选项：
        http://netbeans.org/downloads/

    h. Netbeans Android 插件（0.11 及以上）——使用 Netbeans IDE 时为可
       选项：
        http://kenai.com/projects/nbandroid/pages/Install

===============================================================================
2. 安装设置

    a. 解开 tar.gz 文件：
        tar -zxf fwknop-2.0.4.tar.gz

    b. 用 sdk 和 ndk 工具的路径更新属性文件：
        .../fwknop-android/project/build.properties
            sdk.dir=/home/yourname/tools/android-sdk-linux_86
            ndk.dir=/home/yourname/tools/android-ndk-r5

       如果你运行的是 Android-4.1.2 或更高版本，这些路径应该更像：
            sdk.dir=/home/yourname/tools/android-sdk-linux
            ndk.dir=/home/yourname/tools/android-ndk-r8b

    c. 把 Android SDK 的 platform 目录加入你的用户 PATH 变量：
        export PATH=$PATH:/home/yourname/tools/android-sdk-linux_86/platforms-tools

       对于 Android-4.1.2：
        export PATH=$PATH:/home/yourname/android-sdk-linux/tools: \
        /home/yourname/android-ndk-r8b:/home/yourname/android-sdk-linux/platform-tools

    d. 创建一个新的 Android 设备：在命令行中键入 "android"。
        这会启动 android SDK 和 AVD Manager。
        在 Virtual Devices 下创建一个 AVD 设备，命名为 "Android2.1"。

       对于 Android-4.1.2，把新的 AVD 设备命名为 "Android_fwknop"。

    e. （如有需要）用上面正确的虚拟设备更新项目文件：
        .../fwknop-android/project/nbproject/project.properties
            platform.active=Android2.1

       对于 Android-4.1.2，使用
       .../fwknop-android/project/nbproject/project-4.1.2.properties
       文件（你可能想用它替换 project.properties 文件）。
       同样，Android-4.1.2 用户可能还想用 build-4.1.2.properties 文件
       替换 build.properties 文件。

    f. 相关的 fwknop android C 代码位于 ".../project/jni" 目录：
         1. ".../project/jni/libfwknop" 目录应当包含 fwknop 主目录 lib/
            下的全部 .c 和 .h 文件（即 libfko 源文件。你可以手工把
            .../fwknop.../lib 中的全部 *.[ch] 文件复制过来，或者使用
            "get_libfko_files.sh" 脚本）。

         2. ".../project/jni/fwknop" 目录包含 Android 特有的客户端代码
            （带 Java Native Interface 钩子）。

===============================================================================
3. 构建

    a. 要构建该应用，确保你位于 project 目录下：
        $> cd /home/yourname/.../fwknop-android/project

    b. 构建应用程序，键入：
        $> ant jar

===============================================================================
4. 运行

    a. 现在在 .../fwknop-android/dist 目录下找到 .apk 文件。你可以键入
       以下命令把 .apk 文件复制到 SD 卡上（确保 Android 模拟器已启动）：
            $> adb push Fwknop.apk /sdcard/

    然后你可以用 Astro 之类的文件管理器在 Android 设备内打开并安装它。
    确保你已在应用程序设置中打开「未知来源」选项。

===============================================================================
5. Android 模拟器

    如果你没有实体设备，可以在 Android 模拟器中安装并运行 Fwknop。步骤
    如下：
        $> android

    在虚拟设备中找到你的设备 "Android2.1" 并启动它。

    在命令行中键入：
        $> adb install Fwknop.apk

    现在你可以在菜单中找到 Firewall Knock Operator 应用，点击图标即可
    开始使用。

===============================================================================
6. 用 Netbeans 开发

要用 Netbeans 开发和构建，请确保已安装 nbandroid 插件（见需求部分）。
安装好之后，在 Tool -> Java Platforms 下添加一个新的 Java 平台，选择
Google Android 平台，定位到你在第 1e 步下载的 SDK 平台目录。最后，直接
把 .../fwknop-android/project 目录作为 Netbeans 项目打开即可。

===============================================================================
7. 调试

你可以用 gdb 调试该应用。在此之前，确保你的 AndroidManifest.xml 文件中
有以下属性：
    android:debuggable="true"
然后重新构建应用并启动 Android 模拟器，再在命令行中键入：
    $> ndk-gdb --project=<path-to>/android/project

这将以远程调试方式启动 gdb；类似地，你可以按相同步骤调试实体设备。

===============================================================================
更新记录
v0.9 2010 年 12 月 28 日
1) Rijndael 客户端支持
2) 本地与外部 IP 解析
2) 自动启动 ConnectBot
