安装说明
*********

版权所有 (C) 1994, 1995, 1996, 1999, 2000, 2001, 2002, 2004, 2005,
2006 自由软件基金会（Free Software Foundation, Inc.）

本文件是自由文档；自由软件基金会授予复制、分发和修改它的无限许可。

（本文件为 Zurker fork 翻译维护的中文译本；英文原文见 INSTALL.en。）

基本安装
==================

简而言之，shell 命令 `./configure; make; make install' 即可配置、
构建并安装本软件包。以下更详细的说明是通用性的；本软件包特有的说明
见 `README' 文件。

   `configure' shell 脚本会尝试猜测编译过程中使用的各种系统相关变量
的正确值。它用这些值在软件包的每个目录中创建 `Makefile'。它还可能
创建一个或多个包含系统相关定义的 `.h' 文件。最后，它创建一个
shell 脚本 `config.status'，供你日后重新生成当前配置；以及一个
`config.log' 文件，内含编译器输出（主要用于调试 `configure'）。

   它还可以使用一个可选文件（通常叫 `config.cache'，用
`--cache-file=config.cache' 或简单地用 `-C' 启用），保存测试结果
以加快重新配置的速度。缓存默认是禁用的，以防止意外使用过期缓存文件
带来的问题。

   如果你在编译本软件包时需要做一些不寻常的事，请尝试设想
`configure' 可以怎样检查这些事，并把补丁或说明寄到 `README' 中给出
的地址，以便在下一个版本中加以考虑。如果你正在使用缓存，而某时
`config.cache' 中包含了你不想保留的结果，你可以删除或编辑它。

   文件 `configure.ac'（或 `configure.in'）被名为 `autoconf' 的程序
用来生成 `configure'。只有当你想修改它或用更新版本的 `autoconf'
重新生成 `configure' 时，你才需要 `configure.ac'。

编译本软件包最简单的方法是：

  1. 用 `cd' 进入包含软件包源代码的目录，然后键入 `./configure'
     为你的系统配置本软件包。

     运行 `configure' 可能需要一段时间。运行时它会打印一些消息，
     说明正在检查哪些特性。

  2. 键入 `make' 编译软件包。

  3. 可选地，键入 `make check' 运行软件包自带的任何自测试。

  4. 键入 `make install' 安装程序以及任何数据文件和文档。

  5. 你可以键入 `make clean' 从源代码目录中删除程序二进制文件和目标
     文件。若要同时删除 `configure' 创建的文件（以便为另一种计算机
     编译本软件包），键入 `make distclean'。还有一个
     `make maintainer-clean' 目标，但它主要面向软件包的开发者。如果
     使用它，你可能需要安装各种其他程序，才能重新生成发行版自带的
     文件。

编译器与选项
=====================

有些系统需要 `configure' 脚本不知道的、不寻常的编译或链接选项。
运行 `./configure --help' 可查看相关环境变量的详细信息。

   你可以在命令行或环境中设置变量，为 `configure' 提供配置参数的
初始值。示例：

     ./configure CC=c99 CFLAGS=-g LIBS=-lposix

   详见「定义变量」一节。

为多架构编译
====================================

你可以同时为多种计算机编译本软件包，方法是为每种架构把目标文件放在
各自的目录中。为此可以使用 GNU `make'：用 `cd' 进入你希望放置目标
文件和可执行文件的目录，然后运行 `configure' 脚本。`configure' 会
自动检查 `configure' 所在目录及其 `..' 上级目录中的源代码。

   使用非 GNU 的 `make' 时，更稳妥的做法是每次在源代码目录中只为
一种架构编译。为一种架构安装完毕后，先 `make distclean' 再为另一种
架构重新配置。

安装路径名称
==================

默认情况下，`make install' 把软件包的命令安装到 `/usr/local/bin'
之下，头文件安装到 `/usr/local/include' 之下，等等。你可以用
`--prefix=PREFIX' 选项为 `configure' 指定 `/usr/local' 以外的安装
前缀。

   你可以为架构相关的文件和架构无关的文件分别指定安装前缀。如果向
`configure' 传递 `--exec-prefix=PREFIX' 选项，软件包将使用 PREFIX
作为安装程序和库文件的前缀。文档和其他数据文件仍使用常规前缀。

   此外，如果你使用不寻常的目录布局，可以提供 `--bindir=DIR' 之类的
选项，为特定类型的文件指定不同的值。运行 `configure --help' 可查看
可以设置的目录列表及各类文件的归属。

   如果软件包支持，你还可以让安装的程序名带有额外的前缀或后缀，方
法是向 `configure' 传递 `--program-prefix=PREFIX' 或
`--program-suffix=SUFFIX' 选项。

可选特性
=================

有些软件包会响应 `configure' 的 `--enable-FEATURE' 选项，其中
FEATURE 表示软件包的可选部分。它们也可能响应 `--with-PACKAGE'
选项，其中 PACKAGE 是诸如 `gnu-as' 或 `x'（X Window 系统）之类的
东西。软件包识别的 `--enable-' 和 `--with-' 选项应当在 `README'
中提及。

   对于使用 X Window 系统的软件包，`configure' 通常能自动找到 X 的
头文件和库文件；如果不能，你可以用 `--x-includes=DIR' 和
`--x-libraries=DIR' 选项指定它们的位置。

指定系统类型
==================

有些特性 `configure' 无法自动判断，需要根据软件包将要运行的机器
类型来确定。通常，假设构建和运行在同一类架构上进行，`configure'
能够自行判断；但如果它打印消息说无法猜测机器类型，请给它
`--build=TYPE' 选项。TYPE 可以是系统类型的短名称（如 `sun4'），
也可以是具有如下形式的规范名称：

     CPU-厂商-系统

其中「系统」部分可以是如下形式之一：

     操作系统 内核-操作系统

   各字段的可能取值见 `config.sub' 文件。如果本软件包未包含
`config.sub'，那么本软件包不需要知道机器类型。

   如果你是在_构建_用于交叉编译的编译器工具，应使用
`--target=TYPE' 选项来选择它们将为之生成代码的系统类型。

   如果你要_使用_交叉编译器（即为与构建平台不同的平台生成代码），
请用 `--host=TYPE' 指定「宿主」平台（即生成的程序最终将在其上运行
的平台）。

共享默认值
================

如果你想为各个 `configure' 脚本设置共享的默认值，可以创建一个名为
`config.site' 的站点 shell 脚本，在其中为 `CC'、`cache_file' 和
`prefix' 等变量给出默认值。`configure' 会先查找
`PREFIX/share/config.site'（若存在），再查找
`PREFIX/etc/config.site'（若存在）。你也可以把 `CONFIG_SITE'
环境变量设为站点脚本的位置。警告：并非所有 `configure' 脚本都会
查找站点脚本。

定义变量
==================

未在站点 shell 脚本中定义的变量，可以在传递给 `configure' 的环境中
设置。不过，有些软件包在构建期间可能再次运行 configure，这些定制的
变量值可能会丢失。为避免此问题，应当在 `configure' 命令行中用
`VAR=value' 的形式设置。例如：

     ./configure CC=/usr/local2/bin/gcc

会将指定的 `gcc' 用作 C 编译器（除非被站点 shell 脚本覆盖）。

遗憾的是，由于 Autoconf 的一个缺陷，这一技巧对 `CONFIG_SHELL' 不起
作用。在该缺陷修复之前，可以使用如下变通方法：

     CONFIG_SHELL=/bin/bash /bin/bash ./configure CONFIG_SHELL=/bin/bash

`configure' 的调用
======================

`configure' 识别以下用于控制其行为的选项。

`--help'
`-h'
     打印 `configure' 各选项的摘要，然后退出。

`--version'
`-V'
     打印用于生成 `configure' 脚本的 Autoconf 版本，然后退出。

`--cache-file=FILE'
     启用缓存：把测试结果使用并保存到 FILE，传统上为
     `config.cache'。FILE 默认为 `/dev/null'，即禁用缓存。

`--config-cache'
`-C'
     `--cache-file=config.cache' 的别名。

`--quiet'
`--silent'
`-q'
     不打印正在做哪些检查的消息。要抑制所有正常输出，将其重定向到
     `/dev/null'（错误消息仍会显示）。

`--srcdir=DIR'
     在目录 DIR 中查找软件包的源代码。通常 `configure' 能自动确定
     该目录。

`configure' 还接受其他一些不太常用的选项。运行 `configure --help'
可了解更多细节。
