'\" t
.\"     Title: fwknopd
.\"    Author: [see the "AUTHORS" section]
.\" Generator: DocBook XSL Stylesheets v1.79.1 <http://docbook.sf.net/>
.\"      Date: 08/06/2018
.\"    Manual: Fwknop Server
.\"    Source: Fwknop Server
.\"  Language: English
.\"
.TH "FWKNOPD" "8" "08/06/2018" "Fwknop Server" "Fwknop Server"
.\" 本手册页为 Zurker fork 翻译维护的中文版本，英文原文见同目录 fwknopd.en.8 副本。
.\" -----------------------------------------------------------------
.\" * Define some portability stuff
.\" -----------------------------------------------------------------
.\" ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
.\" http://bugs.debian.org/507673
.\" http://lists.gnu.org/archive/html/groff/2009-02/msg00013.html
.\" ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
.ie \n(.g .ds Aq \(aq
.el       .ds Aq '
.\" -----------------------------------------------------------------
.\" * set default formatting
.\" -----------------------------------------------------------------
.\" disable hyphenation
.nh
.\" disable justification (adjust text to left margin only)
.ad l
.\" -----------------------------------------------------------------
.\" * MAIN CONTENT STARTS HERE *
.\" -----------------------------------------------------------------
.SH "名称"
fwknopd \- Firewall Knock Operator 守护进程
.SH "语法"
.sp
\fBfwknopd\fR [\fI选项\fR]
.SH "描述"
.sp
\fBfwknopd\fR 是 FireWall Knock Operator 的服务端组件，负责监听并处理由 \fBfwknop\fR 客户端生成的单包授权（Single Packet Authorization，SPA）数据包，在对合法 SPA 数据包进行认证和解密之后（按此顺序），修改防火墙或 ACL 策略以放行所请求的访问，并在可配置的超时之后移除该访问权限。
.sp
本程序的主要用途是为诸如 \fISSH\fR 之类的服务增加一层额外的安全隐蔽，使漏洞（包括 0\-day 和未打补丁的代码）的利用变得大为困难。此外，以这种方式隐藏的服务天然无法被 \fINmap\fR 或 \fIShodan\fR 扫描到。
.sp
\fBfwknopd\fR 的主要配置保存在两个文件中：\fIfwknopd\&.conf\fR 和 \fIaccess\&.conf\fR。这些文件的默认位置在软件包配置时确定（通常为 \fI@sysconfdir@/fwknop\fR）。这些文件中的配置变量将在下文中说明。
.sp
更多信息可参见教程 \(lqSingle Packet Authorization: A Comprehensive Guide to Strong Service Concealment with fwknop\(rq，可在线获取（参见：\fIhttp://www\&.cipherdyne\&.org/fwknop/docs/fwknop\-tutorial\&.html\fR）。
.SH "命令行选项"
.PP
\fB\-i, \-\-interface\fR=\fI<interface>\fR
.RS 4
手动指定用于嗅探的接口，例如 \(lq\-i eth0\(rq。通常不需要此选项，因为
\fIfwknopd\&.conf\fR
文件中的 \(lqPCAP_INTF\(rq 关键字已定义了嗅探接口。
.RE
.PP
\fB\-f, \-\-foreground\fR
.RS 4
让
\fBfwknopd\fR
在前台运行而不成为守护进程。在前台运行时，本应写入日志的消息将改为发送到 stderr。此模式通常用于测试和/或调试。
.RE
.PP
\fB\-\-fw\-list\fR
.RS 4
仅列出任何正在运行的
\fBfwknopd\fR
守护进程所创建的防火墙规则，然后退出。
.RE
.PP
\fB\-a, \-\-access\-file\fR=\fI<access\-file>\fR
.RS 4
指定
\fIaccess\&.conf\fR
文件的位置。如果未给出此选项，
\fIfwknopd\fR
将使用编译时的默认位置（通常为
\fI@sysconfdir@/fwknop/access\&.conf\fR）。
.RE
.PP
\fB\-\-access\-folder\fR=\fI<access\-folder>\fR
.RS 4
指定
\fIaccess\&.conf\fR
文件夹的位置。如果给出此选项，则不是加载单个 access\&.conf 文件，而是处理给定文件夹中的所有 \&.conf 文件。
.RE
.PP
\fB\-c, \-\-config\fR=\fI<config\-file>\fR
.RS 4
指定
\fIfwknopd\&.conf\fR
文件的位置。如果未给出此选项，
\fIfwknopd\fR
将使用默认位置（通常为
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR）。
.RE
.PP
\fB\-C, \-\-packet\-limit\fR=\fI<n>\fR
.RS 4
指定要处理的候选 SPA 数据包数量，达到该上限后退出。
.RE
.PP
\fB\-d, \-\-digest\-file\fR=\fI<digest\-file>\fR
.RS 4
指定
\fIdigest\&.cache\fR
文件的位置。如果未给出此选项，
\fIfwknopd\fR
将使用编译时的默认位置（通常为
\fI@localstatedir@/fwknop/digest\&.cache\fR）。
.RE
.PP
\fB\-D, \-\-dump\-config\fR
.RS 4
将
\fBfwknopd\fR
从
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR
（或覆盖文件）和
\fI@sysconfdir@/fwknop/access\&.conf\fR
中得出的配置值输出到 stderr。
.RE
.PP
\fB\-\-dump\-serv\-err\-codes\fR
.RS 4
将所有可能的
\fBfwknopd\fR
错误码输出到 stdout 并退出。此选项在实践中很少需要，添加它是为了辅助测试覆盖率统计。
.RE
.PP
\fB\-\-fault\-injection\-tag\fR=\fI<tag>\fR
.RS 4
此选项仅在
\fBfwknop\fR
编译时支持 libfiu 库（参见：
\fIhttp://blitiri\&.com\&.ar/p/libfiu/\fR）的情况下用于故障注入测试。正常情况下不使用此选项，任何打包版本的 fwknop 都不会编入相关代码，因此此能力在运行时不可用。此处仅为完整起见予以记载。
.RE
.PP
\fB\-A, \-\-afl\-fuzzing\fR
.RS 4
指示
\fBfwknopd\fR
直接从 stdin 获取 SPA 数据包，以支持由 Michal Zalewski 编写的
\fIAmerican Fuzzy Lop\fR
（AFL）模糊测试器进行模糊测试。这要求
\fBfwknop\fR
以传给
\fBconfigure\fR
脚本的
\fI\-\-enable\-afl\-fuzzing\fR
参数编译，因为这样能以 AFL 正常运行所需的方式对加密/摘要进行短路处理。该策略的好处是 AFL 可以对
\fBlibfko\fR
实现的 SPA 数据包解码例程进行模糊测试。
.RE
.PP
\fB\-\-fw\-list\-all\fR
.RS 4
列出所有防火墙规则，包括与
\fBfwknopd\fR
无关的规则。
.RE
.PP
\fB\-\-fw\-flush\fR
.RS 4
清除正在运行的
\fBfwknopd\fR
进程所创建的任何防火墙规则。此选项让用户可以轻松删除
\fBfwknopd\fR
防火墙规则，而不必等待它们超时。
.RE
.PP
\fB\-K, \-\-kill\fR
.RS 4
终止当前的
\fBfwknopd\fR
进程。这提供了一种快速简便的停止
\fBfwknopd\fR
的方式，无需查看进程表。
.RE
.PP
\fB\-\-exit\-parse\-config\fR
.RS 4
解析配置文件（\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR 和
\fI@sysconfdir@/fwknop/access\&.conf\fR）然后退出。这提供了一种在不开始处理网络流量的情况下测试配置文件结构是否正确的方法。
.RE
.PP
\fB\-\-exit\-parse\-digest\-cache\fR
.RS 4
解析摘要缓存文件
\fI@localstatedir@/fwknop/digest\&.cache\fR
然后退出。这在不开始处理网络流量的情况下验证摘要缓存文件的结构。注意，此模式下也会解析标准配置文件。
.RE
.PP
\fB\-l, \-\-locale\fR=\fI<locale>\fR
.RS 4
设置/覆盖系统默认的 locale 设置。
.RE
.PP
\fB\-\-no\-ipt\-check\-support\fR
.RS 4
禁用 iptables
\fI\-C\fR
选项的使用。通常不需要此选项，仅在较旧的、iptables 不支持
\fI\-C\fR
的 Linux 发行版上有用。
.RE
.PP
\fB\-O, \-\-override\-config\fR=\fI<file>\fR
.RS 4
用指定文件中的值覆盖通常从
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR
文件读取的配置变量值。多个覆盖配置文件可以以逗号分隔的列表形式给出。
.RE
.PP
\fB\-\-key\-gen\fR
.RS 4
让
\fBfwknopd\fR
生成可用于 SPA 数据包加密和认证的 Rijndael 与 HMAC 密钥。这些密钥派生自 /dev/urandom，经 base64 编码后打印到 stdout，需要手动加入
\fI@sysconfdir@/fwknop/access\&.conf\fR
文件中的某个节（stanza）。此类密钥通常比口令（passphrase）更安全。
.RE
.PP
\fB\-\-key\-gen\-file\fR=\fI<file>\fR
.RS 4
将生成的密钥写入指定文件。注意，如果文件已存在则会被覆盖。如果未给出此选项，则
\fB\-\-key\-gen\fR
将密钥写入 stdout。
.RE
.PP
\fB\-\-key\-len\fR=\fI<length>\fR
.RS 4
指定生成的 Rijndael 密钥的字节数。当前最大为 128 字节。
.RE
.PP
\fB\-\-hmac\-key\-len\fR=\fI<length>\fR
.RS 4
指定生成的 HMAC 密钥的字节数。当前最大为 128 字节。
.RE
.PP
\fB\-p, \-\-pid\-file\fR=\fI<pid\-file>\fR
.RS 4
指定
\fIfwknopd\&.pid\fR
文件的位置。如果未给出此选项，
\fIfwknopd\fR
将使用编译时的默认位置（通常为
\fI@localstatedir@/fwknop/fwknopd\&.pid\fR）。
.RE
.PP
\fB\-P, \-\-pcap\-filter\fR=\fI<filter>\fR
.RS 4
在
\fBfwknopd\fR
命令行上指定 Berkeley 包过滤器语句。这会覆盖从
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR
文件中取得的 PCAP_FILTER 变量值。
.RE
.PP
\fB\-\-pcap\-file\fR=\fI<pcap\-file>\fR
.RS 4
此选项指示
\fBfwknopd\fR
从 pcap 文件读取数据包数据，而不是直接嗅探接口。此模式通常用于调试目的，并且会禁用 SPA 数据包时效检查，除非在
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR
文件中手动启用。
.RE
.PP
\fB\-\-pcap\-any\-direction\fR
.RS 4
允许
\fBfwknopd\fR
嗅探 SPA 数据包，无论它们是在嗅探接口上接收的还是从嗅探接口发出的。后一种情况下，当 fwknopd 需要嗅探经某系统转发、目的地为另一网络的 SPA 数据包时此选项很有用。如果嗅探接口是此类数据包的出接口（即 SPA 数据包由该接口发出而非接收），则需要使用此选项
\fBfwknopd\fR
才能看到它们。默认仅嗅探在嗅探接口上接收的数据包。注意，此设置与混杂（promiscuous）模式无关。
.RE
.PP
\fB\-R, \-\-restart\fR
.RS 4
重启当前正在运行的
\fBfwknopd\fR
进程。此选项将保留提供给原
\fBfwknopd\fR
进程的命令行选项，但会强制
\fBfwknopd\fR
重新读取
\fIfwknopd\&.conf\fR
和
\fI@sysconfdir@/fwknop/access\&.conf\fR
文件。这还会强制清除当前的 \(lqFWKNOP\(rq iptables 链。
.RE
.PP
\fB\-\-rotate\-digest\-cache\fR
.RS 4
通过将摘要缓存文件重命名为 \(lq<name>\-old\(rq 并开始一个新文件来轮转摘要缓存文件。摘要缓存文件通常位于
\fI@localstatedir@/fwknop/digest\&.cache\fR。
.RE
.PP
\fB\-r, \-\-run\-dir\fR=\fI<path>\fR
.RS 4
指定
\fBfwknopd\fR
写入运行时状态文件的目录。默认为
\fI/usr/local/var\fR。
.RE
.PP
\fB\-S, \-\-status\fR
.RS 4
显示任何可能在运行或可能未运行的
\fBfwknopd\fR
进程的状态。如果存在正在运行的 fwknopd 进程，则退出状态返回 0，否则返回 1。
.RE
.PP
\fB\-\-syslog\-enable\fR
.RS 4
即使设置了前台模式，也允许将消息发送到 syslog。
.RE
.PP
\fB\-t, \-\-test\fR
.RS 4
以测试模式运行
\fBfwknopd\fR。这会指示
\fBfwknopd\fR
获取并处理 SPA 数据包，但不操纵防火墙规则，也不执行 SPA 客户端提供的命令。此选项主要用于测试套件中的模糊测试，以确保在不利条件下获得广泛的代码覆盖率。
.RE
.PP
\fB\-U, \-\-udp\-server\fR
.RS 4
以 UDP 服务器模式运行
\fBfwknopd\fR，从而直接通过 UDP 套接字获取 SPA 数据包，而无需使用 libpcap。更多信息见下面对 \(lqENABLE_UDP_SERVER\(rq 配置变量的讨论。
.RE
.PP
\fB\-v, \-\-verbose\fR
.RS 4
以详细模式运行
\fBfwknopd\fR。此选项可多次指定，以提高输出到系统日志文件（或前台运行时输出到屏幕）的详细程度。
.RE
.PP
\fB\-h, \-\-help\fR
.RS 4
显示用法信息并退出。
.RE
.PP
\fB\-V, \-\-Version\fR
.RS 4
显示版本信息并退出。
.RE
.SH "FWKNOPD 配置与访问控制变量"
.sp
\fBfwknopd\fR 从 \fI@sysconfdir@/fwknop/fwknopd\&.conf\fR 文件读取配置变量，用于定义运行参数（嗅探哪个网络接口和端口、启用/禁用哪些特性等）。\fIfwknopd\&.conf\fR 文件不定义任何访问控制指令，也不设置任何加密或认证密钥。
.sp
访问控制指令包含在 \fI@sysconfdir@/fwknop/access\&.conf\fR 文件中。访问控制指令定义加密密钥，以及授予生成了相应加密 SPA 消息的 fwknop 客户端的访问级别。
.SS "FWKNOPD\&.CONF 变量"
.sp
本节列出 \fBfwknopd\fR 使用的较重要的配置变量。你应当检查这些变量以确保它们具有合适的取值，但对大多数系统而言已提供了合理的默认值。更多细节见 \fI@sysconfdir@/fwknop/fwknopd\&.conf\fR 文件。
.PP
\fBPCAP_INTF\fR \fI<interface>\fR
.RS 4
指定
\fBfwknopd\fR
将在其上嗅探数据包的以太网接口。
.RE
.PP
\fBENABLE_PCAP_PROMISC\fR \fI<Y/N>\fR
.RS 4
默认情况下
\fBfwknopd\fR
将 pcap 接口置于混杂模式。设为 \(lqN\(rq 可禁用该行为（非混杂模式）。
.RE
.PP
\fBPCAP_FILTER\fR \fI<pcap filter spec>\fR
.RS 4
定义
\fIPCAP\fR
模式使用的过滤器；
\fBfwknopd\fR
默认为 UDP 端口 62201。但是，如果
\fBfwknop\fR
客户端使用
\fB\-\-rand\-port\fR
选项经由随机端口发送 SPA 数据包，则此变量应更新为类似 \(lqudp dst portrange 10000\-65535\(rq 的值。
.RE
.PP
\fBENABLE_SPA_PACKET_AGING\fR \fI<Y/N>\fR
.RS 4
指示
\fBfwknopd\fR
不接受时间戳过旧的 SPA 数据包。\(lq过旧\(rq的取值由 \(lqMAX_SPA_PACKET_AGE\(rq 变量定义。如果 \(lqENABLE_SPA_PACKET_AGING\(rq 设为 \(lqN\(rq，
\fBfwknopd\fR
将完全不使用客户端时间戳。
.RE
.PP
\fBMAX_SPA_PACKET_AGE\fR \fI<seconds>\fR
.RS 4
定义一个 SPA 数据包可被接受的最大时效（以秒计）。这要求客户端系统与
\fBfwknopd\fR
服务器系统保持较为接近的时间同步（使用 NTP 即可）。默认时效为 120 秒（两分钟）。
.RE
.PP
\fBENABLE_DIGEST_PERSISTENCE\fR \fI<Y/N>\fR
.RS 4
跟踪
\fBfwknopd\fR
先前处理过的 SPA 数据包所关联的摘要值。这使摘要值能在
\fBfwknopd\fR
的多次执行之间保持持久。默认为 \(lqY\(rq。若设为 \(lqN\(rq，
\fBfwknopd\fR
将不会把收到的 SPA 数据包数据与任何先前保存的摘要进行比对。建议保持此特性开启，以降低遭受重放攻击的可能性。
.RE
.PP
\fBRULES_CHECK_THRESHOLD\fR \fI<count>\fR
.RS 4
定义在运行"深度"检查之前，防火墙规则到期时间必须被检查的次数。这使
\fBfwknopd\fR
能够移除带有正确
\fI\fIexp\fR\fR\fI<time>\fR
的规则，即使这些规则是由第三方程序而非
\fBfwknopd\fR
添加的。此变量的默认值为 20，通常会使该检查每两秒左右运行一次。要完全禁用此类检查，请将此变量设为零。
.RE
.PP
\fBENABLE_IPT_FORWARDING\fR \fI<Y/N>\fR
.RS 4
允许 SPA 客户端请求经由 iptables 防火墙访问其后方的服务，而不仅仅是访问防火墙本身（即经由 FWKNOP_FORWARD 链而非 INPUT 链进行访问）。
.RE
.PP
\fBENABLE_IPT_LOCAL_NAT\fR \fI<Y/N>\fR
.RS 4
允许 SPA 客户端请求经由 NAT 访问本地套接字。这仍会在 FWKNOP_INPUT 链中放入一条 ACCEPT 规则，但通过 DNAT 规则将另一个端口转换到真实端口。因此，用户需执行 \(lqssh \-p <port>\(rq 来访问本地服务（参见
\fB\-\-NAT\-local\fR
和
\fB\-\-NAT\-rand\-port\fR
以及
\fBfwknop\fR
客户端命令行）。
.RE
.PP
\fBENABLE_IPT_SNAT\fR \fI<Y/N>\fR
.RS 4
设为 \(lqY\(rq 以启用相应的 SNAT 规则。默认情况下，如果启用了转发访问（见上面的 \(lqENABLE_IPT_FORWARDING\(rq 变量），
\fBfwknopd\fR
会为传入连接创建 DNAT 规则，但不会同时补充 SNAT 规则。在某些情况下，内部系统可能没有返回传入连接源地址的路由，因此还需要应用 SNAT 规则，使内部系统看到的是运行
\fBfwknopd\fR
的内部接口的 IP。
.RE
.PP
\fBSNAT_TRANSLATE_IP\fR \fI<ip_address>\fR
.RS 4
指定用于 SNAT 的 IP 地址。此功能仅在 \(lqENABLE_IPT_SNAT\(rq 设为 \(lqY\(rq 时启用。默认情况下 SNAT 规则使用 MASQUERADE 目标构建（因为这样内部 IP 就不必在此处的
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR
文件中定义），但如果你希望
\fBfwknopd\fR
使用 SNAT 目标，则还必须用 \(lqSNAT_TRANSLATE_IP\(rq 变量定义一个 IP 地址。注意，此变量一般已被弃用，建议使用
\fI@sysconfdir@/fwknop/access\&.conf\fR
文件中的 \(lqFORCE_SNAT\(rq 变量，它可对 SNAT IP 进行按节（per\-stanza）控制。
.RE
.PP
\fBENABLE_IPT_OUTPUT\fR \fI<Y/N>\fR
.RS 4
向 FWKNOP_OUTPUT 链添加 ACCEPT 规则。通常仅在没有状态跟踪规则允许连接应答出站、且 OUTPUT 链采用默认丢弃策略时才有用。
.RE
.PP
\fBMAX_SNIFF_BYTES\fR \fI<bytes>\fR
.RS 4
指定每帧嗅探的最大字节数。默认为 1500。
.RE
.PP
\fBFLUSH_IPT_AT_INIT\fR \fI<Y/N>\fR
.RS 4
在
\fBfwknopd\fR
启动时清除 fwknop 链中的所有现有规则。默认为 \(lqY\(rq。
.RE
.PP
\fBFLUSH_IPT_AT_EXIT\fR \fI<Y/N>\fR
.RS 4
在
\fBfwknopd\fR
停止或以其他方式正常退出时清除 fwknop 链中的所有现有规则。默认为 \(lqY\(rq。
.RE
.PP
\fBEXIT_AT_INTF_DOWN\fR \fI<Y/N>\fR
.RS 4
当
\fBfwknopd\fR
正在嗅探某个接口时，如果该接口被管理性地关闭或被拔出，fwknopd 将干净地退出，并假定 systemd 或 upstart 之类的进程监控基础设施会将其重启。但是，如果 fwknopd 不受 systemd、upstart 或任何其他设施监控，可以用 \(lqEXIT_AT_INTF_DOWN\(rq 变量禁用此行为。禁用后，fwknopd 将在被关闭的接口恢复时尝试恢复运行。
.RE
.PP
\fBENABLE_RULE_PREPEND\fR \fI<Y/N>\fR
.RS 4
对于运行 iptables 或 firewalld 的系统，让
\fBfwknopd\fR
将新的 SPA 规则插入到相关链（如 \(lqFWKNOP_INPUT\(rq）的开头，而不是追加到链的末尾。这使新创建的规则优先于旧规则。
.RE
.PP
\fBENABLE_NAT_DNS\fR \fI<Y/N>\fR
.RS 4
允许 fwknopd 解析 NAT 访问消息中的主机名。
.RE
.PP
\fBGPG_HOME_DIR\fR \fI<path>\fR
.RS 4
如果使用 GPG 密钥而非 Rijndael 对称密钥，此项为默认的 GPG 密钥目录。注意，
\fI@sysconfdir@/fwknop/access\&.conf\fR
中的每个访问节可以指定自己的 GPG 目录以覆盖此默认值。如果此处及
\fIaccess\&.conf\fR
节中都未设置，则使用运行
\fBfwknopd\fR
的用户（很可能是 root）的
\fI$HOME/\&.gnupg\fR
目录。
.RE
.PP
\fBGPG_EXE\fR \fI<path>\fR
.RS 4
指定 GPG 的路径，若未设置则默认为
\fI/usr/bin/gpg\fR。
.RE
.PP
\fBLOCALE\fR \fI<locale>\fR
.RS 4
设置 locale（经由 LC_ALL 变量）。可用于覆盖系统默认 locale。
.RE
.PP
\fBENABLE_SPA_OVER_HTTP\fR \fI<Y/N>\fR
.RS 4
允许
\fBfwknopd\fR
从 HTTP 请求中获取 SPA 数据（由 fwknop 客户端在
\fB\-\-HTTP\fR
模式下生成）。注意，启用此项后，需要更新 \(lqPCAP_FILTER\(rq 变量以嗅探 TCP/80 连接上的流量，并且应在与
\fBfwknopd\fR
相同的服务器上运行一个 web 服务器。
.RE
.PP
\fBALLOW_ANY_USER_AGENT\fR \fI<Y/N>\fR
.RS 4
当
\fBfwknopd\fR
被配置为从 HTTP 请求中获取 SPA 数据时（即 \(lqENABLE_SPA_OVER_HTTP\(rq 设为 \(lqY\(rq 时），控制是否要求客户端的 User\-Agent 以字符串 \(lqFwknop\(rq 开头。默认为 \(lqN\(rq，即要求如此；但若设为 \(lqY\(rq，则
\fBfwknopd\fR
将允许传入 SPA 数据包携带任意 User\-Agent 字符串。
.RE
.PP
\fBENABLE_X_FORWARDED_FOR\fR \fI<Y/N>\fR
.RS 4
允许
\fBfwknopd\fR
将经由 HTTP 捕获的 SPA 数据包中的 X\-Forwarded\-for 头部用作源 IP。当通过 HTTP 代理使用 SPA 时会用到此项。
.RE
.PP
\fBENABLE_TCP_SERVER\fR \fI<Y/N>\fR
.RS 4
启用 fwknopd 的 TCP 服务器。这是一个"哑"TCP 服务器，它将在指定的 TCPSERV_PORT 上接受 TCP 连接请求。若设为 "Y"，fwknopd 将 fork 出一个子进程来监听并接受传入的 TCP 请求。该服务器只接受请求，不进行其他通信。这仅是为了让经由 TCP 传入的 SPA 数据包能进入并被 PCAP 检测到。无论如何，连接都会在 1 秒后关闭。注意，fwknopd 仍然仅通过 pcap 获取数据，因此需要更新 PCAP_FILTER 定义的过滤器以包含此 TCP 端口。
.RE
.PP
\fBTCPSERV_PORT\fR \fI<port>\fR
.RS 4
设置"哑"TCP 服务器监听的端口号。该服务器仅在 \(lqENABLE_TCP_SERVER\(rq 设为 \(lqY\(rq 时才会启动。
.RE
.PP
\fBENABLE_UDP_SERVER\fR \fI<Y/N>\fR
.RS 4
启用
\fBfwknopd\fR
的 UDP 服务器。这会指示
\fBfwknopd\fR
直接通过 UDP 套接字获取 SPA 数据包，而无需使用 libpcap。启用此模式时，
\fBfwknop\fR
应当以
\fB\-\-enable\-udp\-server\fR
（传给
\fBconfigure\fR
脚本）编译，从而可以将 libpcap 从依赖中移除。如预期那样，使用 UDP 服务器时，
\fBfwknopd\fR
从不对传入数据包作任何应答，因此以这种模式收集 SPA 数据包是直接嗅探网络的良好替代方案。
.RE
.PP
\fBUDPSERV_PORT\fR \fI<port>\fR
.RS 4
设置 UDP 服务器监听的端口号。该服务器仅在 \(lqENABLE_UDP_SERVER\(rq 设为 \(lqY\(rq 时才会启动。
.RE
.PP
\fBPCAP_DISPATCH_COUNT\fR \fI<count>\fR
.RS 4
设置调用
\fBpcap_dispatch()\fR
时处理的数据包数量。默认为零，因为这样可让
\fBfwknopd\fR
在相应的回调中处理尽可能多的数据包——在该回调中，对通过一组前置检查的数据包调用 SPA 处理例程。但是，如果
\fBfwknopd\fR
运行在使用旧版本 libpcap 的平台上，可能需要将此值改为正的非零整数。更多信息见
\fBpcap_dispatch(3)\fR
手册页。
.RE
.PP
\fBPCAP_LOOP_SLEEP\fR \fI<microseconds>\fR
.RS 4
设置在 pcap 循环中作为 usleep() 参数传入的微秒数。默认为 100000，即十分之一秒。
.RE
.PP
\fBENABLE_PCAP_ANY_DIRECTION\fR \fI<Y/N>\fR
.RS 4
控制是否允许 fwknopd 嗅探 SPA 数据包，无论它们是在嗅探接口上接收的还是从嗅探接口发出的。后一种情况下，当 fwknopd 需要嗅探经某系统转发、目的地为另一网络的 SPA 数据包时此项很有用。如果嗅探接口是此类数据包的出接口，则需要将此变量设为 "Y"，fwknopd 才能看到它们。默认为 "N"，此时 fwknopd 只查找在嗅探接口上接收的 SPA 数据包（注意这与混杂模式无关）。
.RE
.PP
\fBSYSLOG_IDENTITY\fR \fI<identity>\fR
.RS 4
覆盖
\fBfwknopd\fR
记录消息时使用的 syslog 标识（identity）。通常默认值即可。
.RE
.PP
\fBSYSLOG_FACILITY\fR \fI<facility>\fR
.RS 4
覆盖 syslog facility。\(lqSYSLOG_FACILITY\(rq 变量可设为 \(lqLOG_LOCAL{0\-7}\(rq 之一或 \(lqLOG_DAEMON\(rq（默认值）。
.RE
.PP
\fBENABLE_DESTINATION_RULE\fR \fI<Y/N>\fR
.RS 4
控制
\fBfwknopd\fR
是否将防火墙规则的目的字段设置为传入 SPA 数据包上指定的目的地址。这对拥有多个 IP 地址、分别承载不同服务的接口很有用。如果 \(lqENABLE_IPT_OUTPUT\(rq 设为 \(lqY\(rq，则设置防火墙规则的源字段。FORWARD 和 SNAT 规则不受影响，但 DNAT 规则也会设置其目的字段。默认为 \(lqN\(rq，即将目的字段设为 0\&.0\&.0\&.0/0（任意）。
.RE
.PP
\fBFWKNOP_RUN_DIR\fR \fI<path>\fR
.RS 4
指定
\fBfwknopd\fR
写入运行时状态文件的目录。默认为
\fI/usr/local/var\fR。
.RE
.SS "ACCESS\&.CONF 变量"
.sp
本节说明 \fI@sysconfdir@/fwknop/access\&.conf\fR 文件中的访问控制指令。这些指令定义加密与认证密钥，以及授予生成了相应的加密且认证的 SPA 数据包的 \fBfwknop\fR 客户端的访问级别。
.sp
下面描述的 \fIaccess\&.conf\fR 变量，为源 IP（或内嵌请求 IP）与 \(lqSOURCE\(rq 变量所定义的地址或网络范围相匹配的 SPA 数据包提供访问指令。\(lqSOURCE\(rq 之后的所有变量都应用于该源\fI节\fR（stanza）。每个 \(lqSOURCE\(rq 指令开始一个新的节。
.PP
\fBSOURCE\fR \fI<IP,\&.\&.,IP/NET,\&.\&.,NET/ANY>\fR
.RS 4
定义 SPA 数据包将被接受的源地址。如果应接受来自任意源 IP 的合法 SPA 数据包，也可使用字符串 \(lqANY\(rq。
\fI@sysconfdir@/fwknop/access\&.conf\fR
中每个授权节的定义都必须以 \(lqSOURCE\(rq 关键字开头。网络应以 CIDR 记法指定（如 \(lq192\&.168\&.10\&.0/24\(rq），也可指定单个 IP 地址。另外，多个 IP 和/或网络可以定义为逗号分隔的列表（如 \(lq192\&.168\&.10\&.0/24,10\&.1\&.1\&.123\(rq）。
.RE
.PP
\fBDESTINATION\fR \fI<IP,\&.\&.,IP/NET,\&.\&.,NET/ANY>\fR
.RS 4
定义 SPA 数据包将被接受的目的地址。如果应接受发往任意目的 IP 的合法 SPA 数据包，也可使用字符串 \(lqANY\(rq。网络应以 CIDR 记法指定（如 \(lq192\&.168\&.10\&.0/24\(rq），也可指定单个 IP 地址。另外，多个 IP 和/或网络可以定义为逗号分隔的列表（如 \(lq192\&.168\&.10\&.0/24,10\&.1\&.1\&.123\(rq）。
.RE
.PP
\fBOPEN_PORTS\fR \fI<proto/port>,\&...,<proto/port>\fR
.RS 4
定义一组在看到合法敲门序列后将被开放的端口和协议（tcp 或 udp）。如果未设置此项，
\fBfwknopd\fR
将尝试满足 SPA 数据中指定的任何 proto/port 请求（除非其匹配任何 \(lqRESTRICT_PORTS\(rq 条目）。多个条目以逗号分隔。
.RE
.PP
\fBRESTRICT_PORTS\fR \fI<proto/port>,\&...,<proto/port>\fR
.RS 4
定义一组无论传入 SPA 数据包是否合法都明确
\fB不允许\fR
的端口和协议（tcp 或 udp）。多个条目以逗号分隔。
.RE
.PP
\fBKEY\fR \fI<passphrase>\fR
.RS 4
定义用于解密传入 SPA 数据包的对称密钥，该数据包由
\fBfwknop\fR
客户端使用 Rijndael 加密。实际使用的加密密钥派生自标准的 PBKDF1 算法。除非改用 GnuPG（见下面的 GPG 变量），否则所有 SPA 数据包都需要此变量。
.RE
.PP
\fBKEY_BASE64\fR \fI<base64 encoded passphrase>\fR
.RS 4
与上面的
\fBKEY\fR
选项相同，但以 base64 编码字符串的形式指定对称密钥。这允许 base64 解码后的密钥中包含非 ASCII 字符。
.RE
.PP
\fBHMAC_KEY\fR \fI<key>\fR
.RS 4
指定用于 SPA 数据包认证加密的 HMAC 密钥。Rijndael 和 GPG 两种加密模式均支持，并按先加密后认证（encrypt\-then\-authenticate）模型应用。
.RE
.PP
\fBHMAC_KEY_BASE64\fR \fI<base64 encoded key>\fR
.RS 4
以 base64 编码字符串的形式指定 HMAC 密钥。这允许 base64 解码后的密钥中包含非 ASCII 字符。
.RE
.PP
\fBFW_ACCESS_TIMEOUT\fR \fI<seconds>\fR
.RS 4
定义在来自某源 IP 地址的合法敲门序列之后，
\fBfwknopd\fR
通过防火墙授予访问权限的时长。如果未设置 \(lqFW_ACCESS_TIMEOUT\(rq，将自动设为默认的 30 秒超时。
.RE
.PP
\fB%include\fR \fI<file>\fR
.RS 4
让
\fBfwknopd\fR
导入一个额外的
\fIaccess\&.conf\fR
文件。这允许在文件系统的其他位置定义更多访问节，在某些场景下很有优势——例如让非特权用户为 SPA 操作定义自己的加密和认证密钥。这样，用户无需主
\fI@sysconfdir@/fwknop/access\&.conf\fR
文件的写权限即可更换密钥或定义新密钥。
.RE
.PP
\fB%include_folder\fR \fI<directory>\fR
.RS 4
与上面的
\fI%include\fR
选项类似，
\fI%include_folder\fR
指令让
\fBfwknopd\fR
导入指定目录中的所有 \&.conf 文件。命令行上也有对应的
\fIaccess\-folder\fR
选项支持此功能。
.RE
.PP
\fBENCRYPTION_MODE\fR \fI<mode>\fR
.RS 4
指定使用 AES 时的加密模式。默认为 CBC 模式，但也可选择 OFB、CFB 等其他模式。一般建议不要使用此变量，保持默认值。注意，可以指定字符串 \(lqlegacy\(rq，以便使用 2\&.5 之前版本
\fBfwknop\fR
所采用的旧初始化向量策略生成 SPA 数据包。自 2\&.5 版本起，
\fBfwknop\fR
使用 PBKDF1 进行密钥派生。
.RE
.PP
\fBHMAC_DIGEST_TYPE\fR \fI<digest algorithm>\fR
.RS 4
指定传入 SPA 数据包认证所用的摘要算法。必须为
\fBMD5\fR、
\fBSHA1\fR、
\fBSHA256\fR、
\fBSHA384\fR、
\fBSHA512\fR、
\fBSHA3_256\fR 或
\fBSHA3_512\fR
之一。此字段可选，若未指定，则在访问节需要 HMAC 时
\fBfwknopd\fR
默认使用 SHA256。
.RE
.PP
\fBACCESS_EXPIRE\fR \fI<MM/DD/YYYY>\fR
.RS 4
以 MM/DD/YYYY 格式定义访问节的到期日期。匹配已过期节的所有 SPA 数据包都将被忽略。此参数可选。
.RE
.PP
\fBACCESS_EXPIRE_EPOCH\fR \fI<seconds>\fR
.RS 4
以 epoch 时间定义访问节的到期日期；当需要比上面的 ACCESS_EXPIRE 变量所提供的"天"级分辨率更精确的到期时间时很有用。匹配已过期节的所有 SPA 数据包都将被忽略。此参数可选。
.RE
.PP
\fBENABLE_CMD_EXEC\fR \fI<Y/N>\fR
.RS 4
指示
\fBfwknopd\fR
接受授权数据包中包含的完整命令。任何此类命令将以 \(lqCMD_EXEC_USER\(rq 所指定的用户身份在
\fBfwknopd\fR
服务器上执行；若未设置该变量，则以启动
\fBfwknopd\fR
的用户身份执行。
.RE
.PP
\fBENABLE_CMD_SUDO_EXEC\fR \fI<Y/N>\fR
.RS 4
\fBsudo\fR
经由 \(lqsudoers\(rq 文件提供了强大的手段来限制用户可执行的命令集合。启用此特性后（并处于 \(lqENABLE_CMD_EXEC\(rq 模式），来自合法 SPA 数据包的所有传入命令都将被加上 \(lq/path/to/sudo \-u <user> \-g <group>\(rq 前缀，其中 sudo 的路径由 \(lqSUDO_EXE\(rq 变量设置，\(lq<user>\(rq 由 \(lqCMD_SUDO_EXEC_USER\(rq 变量设置（未设置时默认为 \(lqroot\(rq），\(lq<group>\(rq 由 \(lqCMD_SUDO_EXEC_GROUP\(rq 设置（未设置时同样默认为 \(lqroot\(rq）。
.RE
.PP
\fBCMD_EXEC_USER\fR \fI<username>\fR
.RS 4
指定（经由 setuid）将执行 SPA 数据包中所含命令的用户。如果未给出此变量，fwknopd 将以自身运行身份（很可能是 root）执行该命令。如果不需要提升权限，强烈建议将其设为 \(lqnobody\(rq 之类的非 root 用户。
.RE
.PP
\fBCMD_SUDO_EXEC_USER\fR \fI<username>\fR
.RS 4
指定（经由 \(lqsudo \-u <user>\(rq）将执行 SPA 数据包中所含命令的用户。如果未给出此变量，fwknopd 将假定该命令应以 root 身份执行。
.RE
.PP
\fBCMD_EXEC_GROUP\fR \fI<groupname>\fR
.RS 4
指定（经由 setgid）将执行 SPA 数据包中所含命令的组。如果未给出此变量，fwknopd 将以自身运行身份（很可能是 root）执行该命令。如果不需要提升权限，强烈建议将其设为 \(lqnobody\(rq 之类的非 root 组。
.RE
.PP
\fBCMD_SUDO_EXEC_GROUP\fR \fI<groupname>\fR
.RS 4
指定（经由 \(lqsudo \-g <group>\(rq）将执行 SPA 数据包中所含命令的组。如果未给出此变量，fwknopd 将假定该命令应以 root 组身份执行。
.RE
.PP
\fBCMD_CYCLE_OPEN\fR \fI<command>\fR
.RS 4
指定收到合法 SPA 数据包后要执行的命令开启/关闭循环。此指令设置初始命令，应与下面的 \(lqCMD_CYCLE_CLOSE\(rq 变量配合使用。此特性的主要用途是让
\fBfwknopd\fR
能与原生不支持的防火墙或 ACL 交互，并实现与 iptables 等主要支持的防火墙相同的访问模型。即：执行一个命令打开防火墙或 ACL，然后在计时器到期后执行相应的关闭命令。\(lqCMD_CYCLE_OPEN\(rq 和 \(lqCMD_CYCLE_CLOSE\(rq 变量均支持特殊的替换字符串，以便从 SPA 载荷中取值并用于被执行命令的命令行。这些字符串以 \(lq$\(rq 字符开头，包括 \(lq$IP\(rq（从 SPA 载荷解密出的放行 IP）、\(lq$SRC\(rq（\(lq$IP\(rq 的同义词）、\(lq$PKT_SRC\(rq（SPA 数据包网络层头部中的源 IP）、\(lq$DST\(rq（目的 IP）、\(lq$PORT\(rq（放行端口）、\(lq$PROTO\(rq（放行协议）、\(lq$TIMEOUT\(rq（设置客户端超时，如已指定）。
.RE
.PP
\fBCMD_CYCLE_CLOSE\fR \fI<command>\fR
.RS 4
指定与上面 \(lqCMD_CYCLE_OPEN\(rq 变量所设开启命令对应的关闭命令。支持同样的 \(lq$IP\(rq、\(lq$PORT\(rq、\(lq$PROTO\(rq 等字符串替换。此外，还可设为特殊值 \(lqNONE\(rq，使开启命令之后不执行关闭命令。在某些情况下这可能很方便，比如希望并允许无限期访问时。
.RE
.PP
\fBCMD_CYCLE_TIMER\fR \fI<seconds>\fR
.RS 4
设置在多少秒之后执行 \(lqCMD_CYCLE_CLOSE\(rq 中所设的关闭命令。这定义了开启/关闭计时器的间隔。
.RE
.PP
\fBSUDO_EXE\fR \fI<path>\fR
.RS 4
定义 sudo 二进制文件的路径。默认为 \(lq/usr/bin/sudo\(rq。
.RE
.PP
\fBREQUIRE_USERNAME\fR \fI<username>\fR
.RS 4
要求客户端系统在 SPA 数据中编码特定的用户名。此变量可选，若未指定，则忽略 SPA 数据中的用户名数据。
.RE
.PP
\fBREQUIRE_SOURCE_ADDRESS\fR \fI<Y/N>\fR
.RS 4
强制所有 SPA 数据包在加密数据中包含真实的 IP 地址。这使得无法在
\fBfwknop\fR
客户端命令行上使用
\fB\-s\fR
参数，因此要么必须使用
\fB\-R\fR
自动解析外部地址（如果客户端位于 NAT 之后），要么客户端必须知道外部 IP 并通过
\fB\-a\fR
参数设置它。
.RE
.PP
\fBREQUIRE_SOURCE_ADDRESS\fR \fI<Y/N>\fR
.RS 4
\(lqREQUIRE_SOURCE_ADDRESS\(rq 的同义词。
.RE
.PP
\fBFORCE_NAT\fR \fI<IP> <PORT>\fR
.RS 4
对于任何合法的 SPA 数据包，强制将所请求的连接 NAT 到指定的（通常是内部的）IP 和端口。如果有多个内部系统运行着 SSHD 之类的服务，而你希望 access\&.conf 文件中的每个节只透明地访问一个内部系统，此项很有用。这样，多个外部用户可以各自仅通过一个 SPA 密钥直接访问一个内部系统。
.RE
.PP
\fBFORCE_SNAT\fR \fI<IP>\fR
.RS 4
对于任何合法的 SPA 数据包，在由相应（必需的）FORCE_NAT 变量创建的 DNAT 规则之外再添加一条 SNAT 规则。这类似于
\fI@sysconfdir@/fwknop/fwknopd\&.conf\fR
文件中的 \(lqSNAT_TRANSLATE_IP\(rq，区别在于它是按访问节设置的，并且覆盖任何用 \(lqSNAT_TRANSLATE_IP\(rq 设置的值。在由于路由限制、传入的 NAT 连接可能无法得到应答的情况下（即接收 SPA 认证连接的系统其默认路由指向与 SPA 系统本身不同的设备）此项很有用。
.RE
.PP
\fBFORCE_MASQUERADE\fR \fI<Y/N>\fR
.RS 4
类似于 \(lqFORCE_SNAT\(rq 变量，区别在于不必再为 SNAT 规则指定 IP 地址，因为改用 MASQUERADE 目标。
.RE
.PP
\fBFORWARD_ALL\fR \fI<Y/N>\fR
.RS 4
在 NAT 场景下，控制是将所有流量都经
\fBfwknopd\fR
系统转发，还是只转发
\fBfwknop\fR
客户端所请求的到特定服务的连接。
.RE
.PP
\fBDISABLE_DNAT\fR \fI<Y/N>\fR
.RS 4
控制在 FORCE_NAT 场景下是否创建 DNAT 规则。主要与 FORWARD_ALL 变量配合使用，使
\fBfwknopd\fR
本质上充当一个 SPA 网关。即：
\fBfwknop\fR
客户端在通过 DHCP 获得 IP 之后，经由 SPA 获取对更广阔互联网的访问；而在发送 SPA 数据包之前，所有发往互联网的流量默认被阻断。
.RE
.PP
\fBGPG_DECRYPT_ID\fR \fI<keyID>\fR
.RS 4
定义用于解密由
\fBfwknop\fR
客户端加密的 SPA 消息的 GnuPG 密钥 ID。基于 GPG 密钥的认证需要此关键字。客户端上的 GPG 密钥环必须已导入并签署
\fBfwknopd\fR
服务器密钥，反之亦然。客户端可以使用敏感的私人 GPG 密钥，但每个
\fBfwknopd\fR
服务器应拥有专为 fwknop 通信生成的自有 GPG 密钥。原因在于，为使
\fBfwknopd\fR
正常工作，服务器密钥的解密密码必须放在
\fI@sysconfdir@/fwknop/access\&.conf\fR
文件中（它必须能够解密用服务器公钥加密的 SPA 消息）。更多关于 fwknop 配合 GnuPG 密钥使用的信息，参见以下链接：\(lqhttp://www\&.cipherdyne\&.org/fwknop/docs/gpghowto\&.html\(rq。
.RE
.PP
\fBGPG_DECRYPT_PW\fR \fI<decrypt password>\fR
.RS 4
指定由上面 \(lqGPG_DECRYPT_ID\(rq 定义的 gpg 密钥的解密密码。这是基于 gpg 认证的必填字段。
.RE
.PP
\fBGPG_ALLOW_NO_PW\fR \fI<Y/N>\fR
.RS 4
允许
\fBfwknopd\fR
使用没有关联密码的 GnuPG 密钥对。尽管这听起来像是有争议的部署方式，但在自动化环境中它是合理的，因为按照 \(lqhttp://www\&.gnupg\&.org/faq/GnuPG\-FAQ\&.html#how\-can\-i\-use\-gnupg\-in\-an\-automated\-environment\(rq 的说法："通常没有比保存在私钥环本身之上更安全的密码存储方式"。在某些 libgpgme 被迫使用 gpg\-agent 和/或 pinentry 来收集密码的环境中，使用此特性并移除 GnuPG 密钥对的密码很有用。
.RE
.PP
\fBGPG_REQUIRE_SIG\fR \fI<Y/N>\fR
.RS 4
当此项设为
\fIY\fR
时，fwknopd 检查所有经 GPG 加密的 SPA 消息是否带有签名（由发送方密钥签署）。如果传入消息未签名，解密过程将失败。若未设置，默认为
\fIY\fR。
.RE
.PP
\fBGPG_DISABLE_SIG\fR \fI<Y/N>\fR
.RS 4
禁用对传入 SPA 消息的签名验证。不推荐此设置，默认为
\fIN\fR。
.RE
.PP
\fBGPG_IGNORE_SIG_VERIFY_ERROR\fR \fI<Y/N>\fR
.RS 4
设置此项将允许 fwknopd 接受已签名但签名未通过验证的传入 GPG 加密数据包（例如签名者密钥已过期等）。此设置仅在 GPG_REQUIRE_SIG 也设为
\fIY\fR
时适用。
.RE
.PP
\fBGPG_REMOTE_ID\fR \fI<keyID,\&...,keyID>\fR
.RS 4
定义一个 gpg 密钥 ID 列表，凡以
\fBfwknopd\fR
服务器密钥加密的传入 SPA 消息都必须由列表中的密钥签名。这确保通过强密码学机制完成对远程用户的验证。签名验证默认启用，仅当 \(lqGPG_DISABLE_SIG\(rq 设为
\fIY\fR
时才能禁用（不推荐的设置）。多个条目以逗号分隔。
.RE
.PP
\fBGPG_FINGERPRINT_ID\fR \fI<keyID,\&...,keyID>\fR
.RS 4
指定一组完整长度的 GnuPG 密钥指纹，替代由 \(lqGPG_REMOTE_ID\(rq 变量设置的较短密钥标识符。下面是 fwknop 测试套件某个密钥的指纹示例：
\fI00CC95F05BC146B6AC4038C9E36F443C6A3FAD56\fR。
.RE
.PP
\fBGPG_HOME_DIR\fR \fI<path>\fR
.RS 4
定义
\fBfwknopd\fR
服务器使用的 GnuPG 目录路径。如果未在
\fI@sysconfdir@/fwknop/access\&.conf\fR
内指定此关键字，则对于由匹配的
\fIaccess\&.conf\fR
节处理的传入 SPA 数据包，
\fBfwknopd\fR
将默认使用
\fI/root/\&.gnupg\fR
目录存放服务器密钥。
.RE
.PP
\fBGPG_EXE\fR \fI<path>\fR
.RS 4
定义 GnuPG 可执行文件的路径。如果未在
\fI@sysconfdir@/fwknop/access\&.conf\fR
内指定此关键字，则
\fBfwknopd\fR
将默认使用
\fI/usr/bin/gpg\fR。
.RE
.SH "文件"
.PP
\fB@sysconfdir@/fwknop/fwknopd\&.conf\fR
.RS 4
fwknop 的主配置文件。
.RE
.PP
\fB@sysconfdir@/fwknop/access\&.conf\fR
.RS 4
定义所有敲门序列和访问控制指令。
.RE
.SH "依赖"
.sp
\fBfwknopd\fR 需要 \fIlibfko\fR，它通常随源码和二进制发行版一同提供，是 fwknop 项目开发的专用库。
.sp
在数据包嗅探方面，\fBfwknopd\fR 目前需要 libpcap，但未来版本将（可选地）移除此依赖。
.sp
对于 GPG 功能，还必须正确安装并配置 GnuPG 以及 libgpgme 库。
.sp
要利用 \fBfwknopd\fR 守护进程/服务的全部认证与访问管理特性，底层操作系统上需要有正常工作的 iptables、ipfw 或 pf 防火墙。
.SH "诊断"
.sp
通过组合 \fB\-f, \-\-foreground\fR 与 \fB\-v, \-\-verbose\fR 命令行选项，可以让 \fBfwknopd\fR 以调试模式运行。这将禁用守护进程模式执行，并在收到数据包时将详细信息打印到屏幕上的 stderr。
.sp
获取 \fBfwknopd\fR 诊断信息最全面的方式是运行 fwknop 源码中 \fItest/\fR 目录下的测试套件脚本 \fItest\-fwknop\&.pl\fR。该测试套件让 fwknop 经历大量运行时测试，支持 \fIvalgrind\fR，针对 OpenSSL 校验 SPA 加密和 HMAC 结果，甚至还内置了自己的 SPA 通信模糊测试器。
.SH "另见"
.sp
fwknopd(8), iptables(8), pf(4), pfctl(8), ipfw(8), gpg(1), libfko 文档。
.sp
更多关于单包授权（Single Packet Authorization）的信息可参见论文 \(lqSingle Packet Authorization with fwknop\(rq，地址为 \fIhttp://www\&.cipherdyne\&.org/fwknop/docs/SPA\&.html\fR。关于 \fBfwknop\fR 操作与理论的全面教程见 \fIhttp://www\&.cipherdyne\&.org/fwknop/docs/fwknop\-tutorial\&.html\fR。该教程还包含 \fBfwknop\fR 设计方面的信息，对于想了解 fwknop 与其他 SPA 实现有何不同的读者，值得一读。
.sp
\fBfwknop\fR 使用 \fIgit\fR 版本控制系统作为其源代码仓库，并配合 \fIGithub\fR 跟踪问题与里程碑：
.sp
.if n \{\
.RS 4
.\}
.nf
    $ git clone https://github\&.com/mrash/fwknop\&.git fwknop\&.git
.fi
.if n \{\
.RE
.\}
.sp
更多关于单包授权的评论可参见 Michael Rash 的 Twitter：http://twitter\&.com/michaelrash，@michaelrash
.SH "作者"
.sp
\fBfwknop\fR 的主要开发者是 Michael Rash（项目创建者）<mbr@cipherdyne\&.org>、Damien Stuart <dstuart@dstuart\&.org> 以及 Jonathan Bennett <jbennett@incomsystems\&.biz>。
.SH "贡献者"
.sp
fwknop 的这个 \(lqC\(rq 版本源自最初的 Perl 版本，开源社区中许多活跃人士曾为其作出贡献。见 fwknop 源码中的 CREDITS 文件，或访问 \fIhttps://github\&.com/mrash/fwknop/blob/master/CREDITS\fR 查看在线贡献者名单。有几位贡献者值得特别提及，包括：Franck Joncourt、Max Kastanas、Vlad Glagolev、Sean Greven、Hank Leininger、Fernando Arnaboldi 和 Erik Gomez。
.sp
\(lqSingle Packet Authorization\(rq（单包授权）一词由 MadHat 与 Simple Nomad 在 2005 年 BlackHat Briefings 上提出。
.SH "已知缺陷"
.sp
请将缺陷报告发送至 dstuart@dstuart\&.org 或 mbr@cipherdyne\&.org，或在 Github 上提交新 issue（见 \fIhttps://github\&.com/mrash/fwknop\&.git\fR）。也随时欢迎建议和/或评论。更多信息可在 \fBfwknop\fR 邮件列表存档中找到（见：\fIhttps://lists\&.sourceforge\&.net/lists/listinfo/fwknop\-discuss\fR）。
.SH "分发"
.sp
\fBfwknopd\fR 依据 GNU 通用公共许可证（GPL v2+）分发，最新版本可从 \fIhttp://www\&.cipherdyne\&.org\fR 下载。
