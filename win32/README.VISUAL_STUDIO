README.VISUAL_STUDIO
====================

（本文件为 Zurker fork 翻译维护的中文译本；英文原文见
README.VISUAL_STUDIO.en。）

随着我们让 fwknop 适配不同版本的 Visual Studio（VS），我们可能会开始
制作特定版本的解决方案（.sln）和项目（.vcproj）文件副本。虽然较新版
本的 VS 大多能转换旧版本的这些文件，但旧版本无法使用新版本创建的
文件。

解决方案和项目文件是：

    - libfko.sln
    - libfko.vcproj
    - fwknop-client.vcproj

特定版本的副本使用相同的文件名，但加上 ".vs2XXX" 后缀（即 VS2008 对应
libfko.sln.vs2008 等）。

如果本目录包含与你的 VS 版本匹配的解决方案和项目文件，你应当把它们
复制覆盖原始版本后再使用。

如果你的 VS 版本不在现有任何文件的覆盖范围内，你应该可以让它读取某个
旧版本文件并进行转换。如果你这样做了，请考虑把这些文件回馈给 fwknop
项目。
