关于 fwknop 手册页生成的说明：

（本文件为 Zurker fork 翻译维护的中文译本；英文原文见 README.en。）

在找到更好的办法之前，'fwknop'（客户端）和 'fwknopd'（服务端）的
手册页由本目录中的 asciidoc 源文件生成，然后在检入前复制到各自的
目录。

我们使用 "a2x" 命令生成最初的 nroff 格式版本，然后手工编辑稍作
清理。"a2x" 命令为：

    a2x -f manpage  fwknopd.man.asciidoc

这会生成 "fwknopd.8" 手册页。不过，取决于你系统上的 a2x 和/或
asciidoc 配置，你可能需要直接编辑 "fwknopd.8" 文件，删除其中嵌入的
"[FIXME: source/manual]" 字符串。目前我们只是直接删除它们。另外，
有些地方你可能希望若干条目连续排列、行间不留空行（例如生成的手册页
中的 AUTHORS 一节）。这种情况下，只需把这些行之间的 ".sp" 改成
".br" 即可。

生成 HTML 版本的文档：

下面是用于创建/生成 fwknop 文档 HTML 版本的一些命令。

对于 libfko API 文档，直接执行 "make html" 即可创建包含一组合理
HTML 页面的 libfki.html 目录。也可以使用 "texi2html"，它更灵活一些。
例如：

    texi2html --noheader --nomenu --toc-links libfko.texi

会生成单个 HTML 文档。这样目录（TOC）被放在文档末尾，但编辑文件把
它移到开头并不难。

要创建手册页的 HTML 版本，只需给 "a2x" 命令加上 "-f xhtml" 选项：

    a2x -f xhtml fwknopd.man.asciidoc
